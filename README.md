# LitHarness

An autonomous book-production system with a human director. See `PLAN.md` for the
master plan and `plan/` for companion design documents.

Stage 0 in progress: the model-free manuscript spine (IR, immutable revisions, bounded
patches, events, jobs, outbox, restore).
