# Attention before the answer is visible

2026-09-07, registered before model calls. The operator asked to fix the remaining issue
after the persistent-concern comparison. That proposal's return followed the source answer;
the resulting chapters interwove attention with explanation. Those observations motivate this
diagnostic, not a causal conclusion. Earlier outputs and reading notes never enter generation.

## Comparison

Derive three tentative anticipations from strictly prefix-only inputs. Each proposer sees
facts available before a fixed incoming event, including explicitly reviewed opening-known
context, and cannot see the incoming unit, later units, original prose, endpoint, display
inventory, source notes or other proposals. The source fixture chooses three boundaries:
before the opening disturbance, before giving instructions, and before looking across the
remaining group. These are experimental sampling choices in the ignored fixture, not narrative
rules in the generic runner. They were chosen before proposals or new drafts existed.

Each card contains anticipation and watch_for (1-20 words each) and 1-5 available basis IDs.
The first is a tentative working expectation; the second is a particular to attend to. No
after-event meaning, future action plan, new biography, motive, rule or incident is requested.
Ordinary continuation and uncertainty are acceptable. The orchestrator assigns before; the
model cannot move the boundary. Reference containment does not certify semantic compatibility.

Compare four fresh complete chapters: two source-only and two with the three cards added.
The common system explains optional cards in both conditions. The only request difference is
prospective_attention. Both receive identical 42-unit source, 31 required-narration IDs,
eleven true but optionally implicit units, display words/order, scene division and ending.
Writer units use canonical id/kind/when/text, excluding competing source_id metadata.
Proposers use a reviewed id/kind/text view with all timing metadata removed: annotations on
earlier facts can mention the incoming event. The ignored preparation trims such a future
reference from one opening-known fact and expands one already-visible display reference
in place. Other fact text and all IDs/kinds/order remain unchanged. These edits were reviewed
before calls; code selects a prefix, while semantic review checks what its text discloses.
Aim 1500-1800 words; retain length misses and source deviations. No original prose, previous
draft, corpus text/digest or reader answer reaches any generator.

Cards apply just BEFORE their incoming event and may shape its encounter. Actual source
observations govern updates; do not hold an expectation true or active after contradicting
evidence. No extra scene, required inner speech, explanatory paragraph, or persistence beyond
the encounter is requested. This tests a guidance package with extra information, planning
and prompt length, not an isolated timing effect, persistence effect or an agent architecture.
Three independent derivations are shared by both guided chapters, not independently repeated
sets. Writers still see the full source: future masking is guaranteed only for proposers.

## Frozen order and gates

Seven sequential subscription Codex CLI calls, requested gpt-6-astra/high:
expectation-1, expectation-2, expectation-3, source-1, prospective-1, prospective-2, source-2.
Stop at 125000 total input + output + separately reported reasoning tokens; cached input is
already included. An in-flight call may cross the stop; none follows. No probe, retry, repair,
redraw, direct API, alternate provider, reset or purchase. Retain every failed slot. An invalid
or unavailable proposal stops the batch without spending writer calls on a failed design.
LITHARNESS_ENV=test blocks every generation role.

Before calls: read the runbook, inspect competing processes, atomically own runs/box.lock,
inspect again, run handoff, and commit code/tests/registration. Freeze source, preparation,
source review, imported code, CLI entry, systems and all three prefix requests. No previous
proposal is sent to a subsequent proposer. Read all proposals against their actual prefix
inputs and source timing before freezing writers. Reject material future knowledge, invented
canon, or impossible attention. A tentative expectation need not predict the real outcome.
Bind all raw-text hashes, all three boundaries and compatibility observations in the review.
Do not treat that review as a quality label. Freeze exact writer inputs and verify equality
except the optional card field before drafting. Separate model calls from sustained checks.

## Reading and reporting

Read all four complete chapters, first paragraph through final status. Locate all 42 source
units per chapter (including optional omissions and qualified realizations), display words
and occurrence order, and attention around each boundary and intervening span. Check:

- Does the opening begin with occupied attention or describe an inventory of source facts?
- Does an anticipation shape perception before/as the event occurs, or arrive as explanation?
- Does the narration update when evidence arrives, instead of forcing the card to stay true?
- Are the card's abstractions copied into thoughts, extra rationales or new factual assertions?
- Do source-only drafts make similar choices without cards? Is exact compound-event timing
  actually demonstrable, or only compatible/ambiguous?

Source-required interpretations are not automatically defects. Locate added restatement,
unsupported inference and omitted events without using counts as quality scores. These agent
readings harvest defects; they are not independent labels, candidate rankings, causal effect
estimates or production qualification. Report OBSERVED, including non-delivery if it occurs,
with hashes and limits. No production prompt or narrative agent is installed from this run.
Retain raw streams/prose under ignored runs, commit source-free code/derived metadata/findings,
browser-check all four outputs, run final handoff, commit/push exact owned files and verify
remote head. Archive only this session's lock when complete.
