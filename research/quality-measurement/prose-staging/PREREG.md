# Source-reviewed action/response staging diagnostic, 2026-09-05

The operator asks to search for a method, with subscription quota only. The preceding Codex
drafts contain interactions connecting routine behavior to consequences, while still showing
explanatory repetition and new consistency problems. Those readings are defect harvest, not
labels or efficacy evidence. This trial tests a CONJECTURE: planning actions, responses and
decisions before prose may change those located behaviors in the original Claude writer.
No model ranks, selects, scores, or accepts a manuscript.

## Procedure fixed before its outputs

Use `runs/ab/prose-framing-20260905/neutral-1.request.json` as the full source request. Keep
its system, rules, private information, required action order, endpoint and 900-word target.
Do not use any previous draft, reading note, example opening or corpus text as an input.

One Codex CLI invocation, `gpt-6-astra` at high reasoning, receives that source and its current
required actions with stable IDs. It returns factual staging notes: action, observable response,
consequence and new information. Fields may be empty where no change occurs. No dialogue quota,
required surprise, sentence pattern or specific narrative is prescribed. Incidental present-scene
staging is permitted; new history, named actors, system announcements, rules and quantities are
not. Conflict reporting is an available outcome. The schema checks coverage and order, not
semantic fidelity or dramatic quality.

Read every proposed step against the source. Record all corrections and reasons before any
draft. Corrections may enforce source facts, knowledge limits and rule-triggered consequences;
they may not import a preferred earlier draft's choices or wording. Freeze the source-reviewed
payload and review note. An unresolved source conflict stops dependent drafting. The intervention
is reviewed staging, not evidence that an autonomous planner is reliable.

Generate four Claude `claude-opus-5` scenes in fixed order: `control-1`, `staged-1`, `staged-2`,
`control-2`. Controls receive the original source exactly. Staged requests add the reviewed
event notes before the existing endpoint section. All original text remains in its original
order; the added notes remain subordinate to source rules and author locks. Every call uses
the preceding framing trial's isolated transport, no built-in tools, empty MCP, manual
permissions and no continuation. Do not redraw or choose a winner.

## Subscription, stops and interpretation

Require Codex login status to report ChatGPT and Claude auth status to report `claude.ai`.
The runner removes API credential and endpoint override environment variables. Codex also
forces ChatGPT login and ignores user config, using the previously tested isolated CLI
settings and amended startup-notice parser. No direct API, account change or quota reset.

At most five new CLI invocations: one planner and four drafts, sequentially. No health
generation, fallback or automatic retry. A request without a result cannot replay. Stop on
failure or missing cost/usage; stop before the next Claude call at $2.50 reported equivalent
Claude quota or 150000 metered tokens across the trial. Codex's separate reasoning field is
included conservatively; its dollar cost is unavailable. A call already running can exceed
the stop. The planner uses its 900-second timeout; drafts retain the source request timeout.
Ordinary tests block fresh provider calls. Hold the box lock for checks and paid calls, with
no heavy tests during generation. Commit registration and call-free controls before running.

Read every draft completely in fixed order. Locate how information reaches characters and
changes action, remaining explanations/comparisons/retrospection, rule conflicts, knowledge
leaks and endpoint changes. More dialogue, fewer words, successful source-ID coverage or
an agent's reading impression cannot qualify this method. No style-count metric is created.
One source story and two drafts per condition cannot establish transfer or literary efficacy.
Retain all prose under ignored runs; commit derived metadata and scoped observations only.
