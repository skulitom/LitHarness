The audit is complete. Everything below was verified read-only (file reads, greps, `Test-Path`, `git ls-files`, `git log -S`); nothing under `results/`, `corpora/` or `derived/` was opened beyond existence checks, and no script was executed.

# Audit findings

## README.md

- **README.md:444** — Text: `uv run litharness … calibrate --metric craft.tricolon_rate.v0 --threshold 4.0 --direction above --precision 0.86 --holdout 50 --flagged 21`. Tree: `src/litharness/cli.py:4788–4797` — `--precision` **no longer exists** on `calibrate`; an in-source comment says "*`--precision` is replaced by counts rather than kept beside them*", replaced by a required `--correct` (cli.py:4792), and the command as printed also omits the now-required `--selection-family` (cli.py:4798–4801). All other flags in the command (`--metric`, `--threshold`, `--direction above` — `Direction.ABOVE = "above"` exists at calibration.py:282 — `--holdout`, `--flagged`) still exist. → **STALE**
- **README.md:702–703** — Text: the pairwise preference engine's verbs are "`corpus-add`, `protocol`, `pools`, `pair-draw`, `pairs`, `pair-judge`, `pair-export`, `pair-import`, `win-rate`". Tree: grep for `pair-export|pair-export|pair_import|pair-import` across all of `src/` returns **0 hits**; `src/litharness/cli.py` registers exactly `corpus-add` (4868), `protocol`, `pools` (4646), `pair-draw` (4919), `pairs`, `pair-judge` (4957), `win-rate` (4981) — no export/import verbs. History confirms removal, not typo: `git log -S 'pair-export' -- src/litharness/cli.py` → commit 65a8ddf "Delete the dead reader-export path and its runbook". → **STALE**

## CONTRIBUTING.md

No discrepancies. Every reference checked out: `[tool.uv.sources]` rev pin in pyproject.toml + uv.lock ✓; `dev`/`corpus` extras ✓; `application/ports.py` with `DraftStore` (643) / `TextGenerator` (908) ✓; `CompletionRequest`/`CompletionResult`/`Usage`/`Resolution` in `domain/generation.py` re-exported by `providers/base.py` ✓; `ProviderRegistry` (providers/registry.py:53) ✓; `SqliteStore` (adapters/sqlite_store.py) with capability repos `sqlite_jobs.py`/`sqlite_plans.py` ✓; `tests/test_architecture.py`, `tests/conftest.py` setting `LITHARNESS_ENV=test` ✓; `LITHARNESS_LIVE_PROVIDERS=1` gating live tests (tests/test_providers.py:557) ✓; CI ≥85% branch coverage (`fail_under = 85`, `branch = true`) and ResourceWarning-as-error ✓; `application.policy_events.policy_decision_event` + `PolicyDecisionRecorded` ✓; BRIEF.md §2 "Pass 6" (BRIEF.md:106) and the tricolon figures 0.629 / 0.606 (BRIEF.md:57–62, matching the "twenty dead" ledger which BRIEF.md:128 itself states) ✓; `corpus_io.generated_scenes` / `by_story` ✓.

## research/quality-measurement/RUNBOOK.md

- **RUNBOOK.md:173–174** — Text: "The dump carries a digest manifest and `--score` refuses a dump whose manifest does not match the fixtures on disk". Tree: `latent_probe.py`'s parser defines only `--extract`, `--rest-ratio`, `--activations`, `--out`, `--fixtures-only` (lines 1048–1057); scoring happens on a **bare** invocation (main() lines 1059+, comment at 1068: "Scoring is cheap and re-run often"), and there is no `--score` option string anywhere in the file — the module docstring (line 47) repeats the same phantom flag. `git log -S '--score'` shows the string arriving only as prose plus a `score()` function (3afc681), never as a registered option. → **WRONG** (never existed as written; scoring is the default mode)
- **RUNBOOK.md:238** — Text: of the `latent_support.py` run, "it imports `cdg_battery.throttle` anyway — same 72/66 hysteresis, same three-failed-reads tolerance, `--rest-ratio` defaulting to the measured-safe 3.0". Tree: `latent_support.py` imports only stdlib modules plus `latent_fixtures` (lines 37–50); grep for `cdg_battery|throttle|rest` over the whole 217-line file returns nothing, and its parser defines a single argument, `--out` (line 229). `git log -S 'cdg_battery'` / `-S 'rest-ratio'` on that file show no commit ever added either. (The 72/66 constants themselves do exist in `cdg_battery.py:109–110`; they are simply not used by this module.) → **WRONG**

Everything else in the RUNBOOK verified clean, including: every script invoked exists; every quoted flag exists in the corresponding argparse parser (persona_battery, summary_reliability incl. the 600-call guard and `level2.gated_by`, writer_states, latent_crossfamily, axiom_battery, promise_kinds, cadence_discrimination incl. `certify`, payoff_landing, bcr incl. `sizing_from_observed`/`failure_kind imprecise|off_centre`/`battery_shelves`/frozen `D1_FAMILIES`/`paragraph_shuffle`, thermal_watch, force_harness `MIN_REFUTING_N = 110`, corpus_leak_audit `--derived-only`, determinism_probe, register_halflife incl. haiku remote family, retention_distance/transmission_chains/compression_progress incl. `ATTENTION_SHAPE`, `qwen3.5-4b`, `EXTENSION_FICTIONS = 200`, `--controls` default 20, force_report states and `--reassemble/--apply`, force_market, platform_priors incl. `register()`→`ablate.BY_KEY` and `shelves()` returning `bcr.Shelf` with `arm="D1P"`, comic_beats incl. `FROZEN_DIGEST = "d3200ddad172e4854b70"`, `CLI_COMMAND_BUDGET` via `list2cmdline`, `CLI_TIMEOUT_SECONDS = 300` in elicit.py:138, `ARMS`, `--library/--toll`, chapter_endings incl. unconditional `selftest()`); named symbols `Elicitor._throttle`, `cdg_battery.throttle` (72/66), `force_gpu.Governor`, `force_remote.Ledger`, `writer_states.Generator`, `_strip_fence`, `axes.strip_system`, `domain/promises.PROMISE_KINDS`, `domain/discrimination.py`, `GoldContextSuite` + `golden_path('litrpg','findings.json')` + the fixture files inside installed litharness_contracts 0.2.0, `StateRecord`; `.gitignore` **line 11 is exactly `*.db`**, `results/*.npz` ignored, `derived/` ignored; result files cited as existing (persona-gate0-tollroad/gate0-fixtures/pairwise-tollroad/pref-gemma/intensity-tollroad/writer-states.json) present; tests `test_closed_form_matches_the_literal_leave_one_scene_out_refit` and the `-k claude_md_from_the_working_directory` target exist; CI commands match ci.yml:39–41; `run_royalroad` splits shards and `royalroad_chapters(shards={3,30}, limit=…)` matches the shard-draw story.

Excluded from judgement as outside this tree: the `C:/DEV/MirrorBench/.venv` interpreter paths and the `../litharness-contracts` checkout path (external checkouts, both labelled as such by the documents themselves).

## Tally

*(This tally is my own count of this audit — what I chose to check and what failed — not a property of the project.)*

- **README.md**: ~75 references checked, **2 discrepancies** (both STALE).
- **CONTRIBUTING.md**: ~30 references checked, **0 discrepancies**.
- **research/quality-measurement/RUNBOOK.md**: ~85 references checked, **2 discrepancies** (both WRONG).