# Initial segment stopped at an experiment-driver error

The registered run produced eight accepted first chapters (12,391 words) across all four
premise pairs. All books completed concept creation, world acceptance, outline, drafting,
evaluation and summary. Every book then stopped before chapter two because the driver passed
the ordinal `1` where `architect grow --scene` requires logical ID `scene-1`. All eight
failures occurred before a provider call and preserved identical before/after metadata.
This is a driver defect, not a writing-workflow finding. No book content was read before
registering the [explicit continuation](../experience-workflow-continuation-20260915/RUNBOOK.md).

The segment used 96 completed provider calls, including health probes, and 2,100,789 recorded
Usage.total tokens. There was no provider failure or unknown usage. The raw usage components,
call and step hashes, attribution checks and retention checks are in evidence.json.
All four updated concepts retained the generated experience brief in their outline requests;
all eight preserved the supplied original brief. All eight stores rebuilt with zero
unattributed revisions. These are data-retention and operational facts, not quality results.

The original auditor incorrectly flagged an adapter default system sentence, reordered
strict-schema required arrays, and an incorrectly called private allowance parser. Its
original evidence.json remains unchanged. The continuation's parent-controls.json records
the corrected checks and retains a hash of this original audit; its RUNBOOK explains the
corrections. No literary reading or comparative conclusion has been made at this checkpoint.
