# Fresh model validation of declared growth and adopted wants

The operator authorized continued autonomous work while away on 2026-09-13. This
isolated engineering experiment tests the production change at `11d3aea`. It does
not qualify a reader, measure literary quality or alter an existing book.

## Assignments fixed before calls

Run five fresh calls in this order, retaining every first output:

1. Wren seed: reuse the exact fixed seed request's source prompt from
   `world-fixed-continuation-20260913/book-1/calls/003.json`, with the installed
   seed v8 system instructions. The supplied Load, Duration and Length investments
   are repeatable without a supplied numeric cap. Inspect whether their declarations
   retain that condition, ownership and rank gates without extra prerequisite depths.
2. Opposing seed: reuse that study's book-2 seed request (`calls/002.json`) with
   seed v8. Fold and Anchor are already owned, later acquisitions and the exclusive
   Mirror/Echo choice remain future. No repeatable depth is supplied. Inspect whether
   the seed preserves these boundaries and completes without inventing deepening.
3. Explicit-cap seed: the Wren request plus one frozen, separately hashed synthetic
   constraint limiting Load to two total investments; other supplied mechanics remain.
   This control tests retention of an explicit cap rather than universal open growth.
4. Conditional goal: the original book-1 Chapter 2 grow request (`calls/010.json`)
   with the installed grow v5 system instructions and unchanged chapter.
5. Adopted goal: the same request with the previous study's frozen conditional-to-adopted
   passage replacement. No extra instruction is appended. Inspect all wants proposals
   against both cases, so suppressing every new pursuit cannot pass the contrast.

Seed fixtures begin with empty world state and the corresponding frozen concept.
Their exact source prompts are retained; no development or invention call intervenes.
Goal fixtures restore the Chapter 1 accepted-world readout by recorded identities and
declaration times, using the read-only original store. Each must reproduce that readout
exactly before calls. Their empty manuscript placeholders are not historical book copies.

Run normal world checks after each call. Declarations remain proposals. Audit completion
and game-system reconstruction without accepting or changing them. For each declared
capability, run a separately labelled in-memory control starting held at one with enough
declared stock to pay two further investments. Test both successive deepening offers and
direct execution; report resulting magnitudes and debits. This counterfactual arithmetic
is not a story event, a model decision or evidence of a completed serial.

## Bounds, isolation and stopping

Freeze production source from `11d3aea`, migrations, lockfile, dependency inventory,
the prior pinned native executable and subscription authentication, all requests,
fixture worlds and experiment code. Exclude the concurrent precision patch. Commit and
push registration, scripts and tests before any model call. Use separate native sessions
and the production restricted bridge, with no transport fallback or session context.

Aggregate ceiling: eight native attempts including health probes, 1,600,000 reported
input plus output tokens, and 90 minutes for starting calls. An in-flight call may
overshoot. No ordinary checker enables live providers. Hold `runs/box.lock` with owner
`growth-declarations-20260913:` for sustained preparation, validation and live calls;
check processes as well as the lock and never overlap another session's sustained job.

Stop live work on unknown usage, transport or containment failure, changed source,
dependencies, native binary, scripts or protected inputs, lost lock, fixture mismatch
or missing attribution. Preserve partial results. Do not retry or implicitly resume.
A semantic or world-coherence failure is an outcome; retain it and continue the other
fixed independent cells if the transport and provenance remain valid.

## Analysis and execution

Record world-check results, typed declarations, effective caps, prerequisite depths,
resource prices, counterfactual moves, goal record locators, complete receipt identities,
native usage and session containment. Preserve full prose privately under ignored
`runs/growth-declarations-20260913/`; commit identifiers, numbers, hashes and bounded
conclusions. These five cells cannot establish population rates or general prompt efficacy.
No raw result becomes feedback in a production manuscript or plan.

```powershell
uv run python research/quality-measurement/growth-declarations-20260913/run.py prepare
uv run python tools/check.py handoff
# Commit and push the registration, runner, audit and regression tests.
runs/growth-declarations-20260913/runtime/Scripts/python.exe research/quality-measurement/growth-declarations-20260913/run.py live
runs/growth-declarations-20260913/runtime/Scripts/python.exe research/quality-measurement/growth-declarations-20260913/audit.py
```

The registered claim concerns whether these fixed first outputs preserve their supplied
growth and goal boundaries. Report observations and any failed control without promoting
them to literary evidence. A follow-up intervention needs its own frozen registration.
