# Declared growth survives the fixed fresh-model controls

All five registered first-output cells completed on production source `11d3aea`,
registered and pushed in `8d81a18` before calls. The three fresh seeds preserve the
tested repeatable, ownership-only and explicitly capped distinctions. The installed
grow prompt preserves the conditional/adopted goal contrast. These are observations
on fixed fixtures, not a population success rate or literary-quality result.

## Growth declarations and opposing controls

| Fixed source | Fresh declarations | Counterfactual arithmetic |
| --- | --- | --- |
| Wren, no supplied investment cap | Load, Duration and Length are `open`, priced at one Point per investment. Loadstitch, Split and Articulated explicitly have limit one. Inspection is independently repeatable. | Starting each paid upgrade held at one with two Points, two further investments reach three and spend both Points. Ownership-only grants and the Point stock offer no deepening. |
| Mara, no supplied repeatable depth | No growth limits are added; the default remains one. Mara stands at Rank Two with Fold and Anchor already owned. Step and the Mirror/Echo fork remain future. | Every capability retains the ownership-only ceiling and refuses deepening. |
| Wren plus synthetic Load cap | Load has limit two. Duration, Length and Inspection remain open; undeclared ownership-only limits retain the default one. | Load permits one further investment from one to two, then stops with one Point left. Duration and Length permit both further investments and spend both Points. |

All three systems complete with default maximum one. Rank prerequisites still refer
to the supplied rungs; capability prerequisites remain ownership at one. The explicit
Load cap does not raise another grant's ceiling. Each fixture's world check is clean,
with no completion refusal or unplaceable record.

The arithmetic controls use freshly returned definitions but invented in-memory
holdings and stock. They are labelled counterfactual in `evidence.json`, leave the
fixture stores unchanged, and are not acquisitions enacted by a generated character.
This experiment drafted no new chapters and accepted no proposed world records.

## Goal contrast under the installed grow prompt

The conditional Chapter 2 passage produced a proposal to continue safe exploration
and accurate observation, with its immediate water pursuit satisfied. It did not
produce a homeward pursuit. The adopted-pursuit counterpart retained the explicit
homeward goal and intention to ask the other arrival, alongside safe work and
accurate observation. Both proposals use the supplied `s000002` story key.

| Cell | New Wren wants record |
| --- | --- |
| Conditional | `rec-w462a0c5a1d4d5a3e55449d40` |
| Adopted | `rec-w36fa5d770ccde86f68b0156a` |

Both world checks are clean. Full returned assertions remain in the corresponding
ignored `wants.private.json` files; `evidence.json` addresses their values and receipts.
The contrast therefore does not pass by suppressing every new goal. There is still
only one fresh output per cell, and no claim that all conditional thoughts or goals
will be handled correctly.

The goal fixtures retain their restored legacy world. They receive no automatic
growth-limit migration. The growth results concern fresh seed declarations, not
retroactive repair of the earlier six-chapter book.

## Execution and provenance

| Assignment | Reported input plus output tokens |
| --- | ---: |
| Native health check | 3,766 |
| Wren seed | 245,457 |
| Opposing seed | 152,240 |
| Explicit-cap seed | 206,373 |
| Conditional grow | 171,357 |
| Adopted grow | 167,623 |
| Total | 946,816 |

The run finished in 721.4 seconds, within the registered ceilings. Six native sessions
are distinct; the five Architect calls use the restricted bridge, and every recorded
tool call is a LitHarness command. The health check uses no commands. The receipts
identify `gpt-6-astra` through the signed-in native Codex provider. All usage reconciles
with the progress record. Original source inputs and stores retain their hashes;
fixture revision attribution checks pass. The concurrent precision patch was excluded
from the frozen production source.

## Exploratory payload measurements after registration

While the fixed calls ran, a separate, call-free prototype measured serialization of
the preceding three-chapter continuation's retained tool replies. It was not a sixth
registered model cell, did not change this experiment, and changed no production
output. `payload_probe.py` rebuilds `payload-probe.json` from addressed source receipts.

All 45 query replies, containing 617 records, round-trip exactly through both layouts:

| Query reply layout | UTF-8 bytes |
| --- | ---: |
| Recorded output | 228,993 |
| Same objects, JSON whitespace removed | 174,848 |
| Shared field names and rows | 128,317 |

Minifying parseable JSON across all replies reduces 315,990 bytes to 259,388 bytes,
about 18%. Non-JSON declaration replies remain identical. Shared columns give a larger
query-only reduction but change the representation the model must interpret. Neither
measurement establishes native-token, latency or monetary savings, or reliable model
behavior. Minification is the smaller next transport change to test prospectively.

## Validation and reproduction

The ten frozen experiment regressions and registration handoff passed before calls.
The payload prototype additionally checks nested values, non-JSON text, empty pages
and refusal to convert absent fields into nulls. The final handoff log is retained at
`runs/growth-declarations-20260913/handoff-final.log`.

```powershell
runs/growth-declarations-20260913/runtime/Scripts/python.exe research/quality-measurement/growth-declarations-20260913/audit.py
uv run python research/quality-measurement/growth-declarations-20260913/payload_probe.py
uv run python research/quality-measurement/epistemic_governance.py research/quality-measurement/growth-declarations-20260913/claim.json
```

The original registration, five requests and frozen scripts remain unchanged. Raw
requests, full worlds, command receipts and native traces stay under the ignored
`runs/growth-declarations-20260913/` root. No reader mechanism was qualified, no
production feedback was issued, and no new long-serial outcome was measured.
