# First-use input follow-up

This is a post-hoc follow-up to the completed twelve-call seeding pilot, within the
operator's request to try varied generation seeds. The original trial is complete and its
inputs and results remain unchanged. Its shadow-duelist pair used the specified power
chiefly to support damaged structures. This follow-up tests a positive first-use instruction
as an additional authoring input. It does not test a quality metric or select a candidate.

Keep packet 2 and all its five ingredients. Append one authored first-use instruction:
the duelist manifests a stolen shadow behind the enemy commander, coordinates with its
original owner to ambush and capture that commander, and needs the captive alive to identify
the revenge oath's target. This success should yield a lead and an escape opportunity, with
the owner's continuing control complicating the fight. No diagnostic text, previous output,
recurring name or thematic ban is sent. This is deliberately more prescriptive; it tests
steering of a specified action, not spontaneous originality or an isolated effect at equal
prompt length. Packet 2 was selected after reading the original trial, so the comparison is
exploratory and must be reported as such.

Two identical prepared requests, two fresh first responses, no redraws. Use the same frozen
source, binary, discovery renderer, schema, gpt-6-astra medium, output request and timeout as
the original trial. Commit the registration before dispatch. Freeze this protocol, runner,
the reused original runner, packet, requests, source and binary. Own the same shared-machine
lock. Stop after two attempts or 22,000 recorded tokens checked before dispatch (an in-flight
call can exceed the bound). Auth, transport, quota, drift or unknown usage stops the batch.
No fallback, retries, API keys, resets or live-provider test flag. Retain complete native
receipts, outputs and validation failures. No book is opened or downstream call made.

Read both outputs after completion or an operational stop. Inspect whether the first
successful power use performs the specified ambush/capture, what the original owner's
control actually does, and whether structural support remains the primary early use.
Retain new repetitions, ignored instructions and incidental maintenance material. Two
successful realizations establish only adherence in these examples. Do not rank the outputs
or infer global originality, reader appeal, distribution-wide novelty or reliable frequency.

Raw material stays under runs/invention-seeding-20260910/action-followup. Commit hashes and
bounded observations. The reusable seed tool is unchanged; the extra action is an explicit
author-brief addition in this follow-up, not a newly qualified production default.
