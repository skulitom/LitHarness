# Scoped disclosure permission comparison

Read this PREREG.md, CONTRIBUTING.md and the shared research RUNBOOK.md, BRIEF.md and
EPISTEMIC_GOVERNANCE.md. Inspect processes, acquire the shared lock atomically, inspect
processes again and announce ownership. Keep sustained validation separate from calls.

Prepare the ignored source with `prepare_source.py`, inspect `input-audit.md`, and check its
exact move against the earlier disclosure inspection. The source script reads JSON only;
do not open or repair the retained original database. Verify the runner's receipt, exact
control identity, inverse edit, same fact count and preservation of other hidden bullets.

```powershell
uv run python tools/check.py handoff
uv run python research/quality-measurement/prose_disclosure_permission.py prepare --out runs/ab/prose-disclosure-permission-20260907 --source runs/ab/prose-disclosure-permission-tools/source.json
uv run python research/quality-measurement/prose_disclosure_permission.py draft --out runs/ab/prose-disclosure-permission-20260907 --slot control-1
```

Commit the runner, tests and registration AFTER handoff and BEFORE prepare. Inspect the
retained transport for each slot before permission-1, permission-2 and control-2 in that order.
The runner stops for identity drift, failed/incomplete calls, internal sampling retry or the
reported token stop. Preserve failures and do not replace a slot or amend the frozen runner.

Read every completed scene without cleanup; retain stable paragraph locations and adverse
findings in the ignored reading record. Expose all slots and exact requests on the comparison
page, including any missing/failed slots. Build source-free execution metadata from the raw
files, preserving reported usage components and failures. Hash local inputs, outputs, reading
artifacts and rebuilding code. Complete RESULTS.md and deviations even when interrupted,
validate the scoped claim, update RESEARCH.md, then run final handoff and commit/push.

Only archive this task's owned lock once validation and calls finish. Existing experimental
registrations, prompts, databases and unrelated local research are not modified.
