# Deviations and qualifications

- All four calls ran in their registered order after the registration commit and input freeze.
  There was no generation deviation, reported internal sampling retry, redraw, incomplete
  slot, direct API use or production mutation. All frozen identities and edit receipts match.
- The first scene is the fixed substrate. No whole chapter or later scene was generated.
- Readings originally used an overly strong description of uninterrupted reading. The source
  says first complete reading and concurrent withdrawal without specifying every timing
  boundary consistently. Final notes locate added acts and their order instead of claiming
  the source literally requires uninterrupted reading.
- An early reading note described an existing postdoc offer as a contradiction of an
  application/hope. Checking the exact source showed that it says the protagonist wants
  the postdoc and wants to take it, without explicitly recording an offer or application.
  READING.md visibly qualifies this as added specificity with ambiguous source implication,
  not a proven contradiction. The independent factual note remains a retained annotation;
  its stronger offer assertion is subject to this qualification.
- The preliminary `comparison.html` was built before that last note qualification.
  It is preserved. `comparison-reviewed.html` embeds the corrected reading record and is
  the delivered page. Neither presentation change altered any request, raw response or
  frozen generation artifact.
- Ordinary output lengths differ around the approximate target. Counts are descriptive.
  Reward visibility and the boundary of an encounter are not fully specified; the noted
  early payments are not promoted to proven timing/audience violations.
- Static reviews and agent agreement do not add observations or confer literary validity.
  The result remains OBSERVED and does not authorize an editorial mechanism or disclosure
  policy. The explicit permission/relocation package does not isolate its components.
- Post-run review found that the initial execution metadata's rebuilding command omitted
  extra-artifact arguments. The existing hashes were correct, but the command would omit
  five audit/helper files. The postprocessor now includes them by default and writes a
  rebuild to a separate path. Running the recorded command reproduced the corrected
  metadata byte for byte. The initial execution record and initial postprocessor are
  preserved under the ignored run/tools roots. No frozen generation input or output changed.
- The final handoff subprocess returned zero and its status was saved. The surrounding
  display wrapper then failed while decoding the log as UTF-8: the Windows child output
  contains a non-UTF-8 byte. The original log bytes are retained. Validation counts are
  recovered from ASCII markers in those bytes; this display failure is separate from the
  successful repository checks.
