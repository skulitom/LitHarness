# Permission to disclose the current scene's fact

2026-09-07. REGISTERED only after commit, before any new generation. This is a bounded
comparison on the first scene of chapter one, not a whole-chapter or literary-quality test.
The [disclosure inspection](../prose-disclosure-debug/RESULTS.md) locates the input conflict
and preserves the original source/provenance and the separate SQLite verification incident.
This experiment reads the archived request and trace JSON; it does not open that database.

## Question and exact intervention

Does explicitly permitting one currently required disclosure change the located redundant
explanations or information delivery in fresh realizations of the original scene? Existing
style tendencies, information density, other source tensions and sampling variation remain
competing explanations. Merely showing the permitted fact cannot establish improvement:
the earlier production-obligations draws already showed it under the original instructions.

Control is the exact archived `request-4.json` from
`runs/ab/chapter-rule-context-20260905`, SHA-256
`4cb3c1292b764ed9087a43fbbd903f9848688a16e1ec3bf15b2d23802722b13f`.
Its system and prompt match the retained earlier scene trace. The request targets 900 words
and the first of two chapter-one scenes. Historical output is not shown to new writers.

Treatment moves only claim `claim_edge`'s bullet out of the hidden block, with its literal
content unchanged, into a separate permission block immediately BEFORE the unchanged hidden
heading. The new heading is exactly:

> May be revealed in this scene; not established as prior reader knowledge:

The character-belief occurrence remains. The literal fact still occurs twice. The seven
other hidden bullets, all text outside the original block, system, scene plan, disclosures
elsewhere, time windows, required display values, endpoint and word target are unchanged.
No instruction about explanation, prose style, attention or literary evaluation is added.
The edit is reversible by removing the inserted block and returning the exact bullet to
its recorded original offset. The ignored input audit records the diff and witnesses; the
runner verifies offsets, preservation, occurrence counts and the transformation receipt.

This is a permission-and-relocation package. Removal of the contradiction, placement,
salience, the added heading and prompt length cannot be attributed separately. No delete-only
arm is registered. Nothing infers permission automatically or marks reader disclosure as
already accomplished. No database, production prompt builder or manuscript is changed.

## Dispatch and retention

Four fresh independent calls in fixed order: control-1, permission-1, permission-2, control-2.
Two draws per condition are replications within one scene fixture, not independent books.
Use the signed-in Codex CLI helper, requested gpt-6-astra/high, identical isolated settings
for every slot. The fresh controls are the transport-matched comparator; historical Claude
Code generation is provenance only. Full upstream instructions and the resolved model are
not captured. Retain source timeout 300 as metadata; the helper's timeout is 900 seconds.

Commit runner, tests and registration, then freeze source, input audit, preparation script,
original request, system files, helper/dependency code and transport identities before calls.
Hold `runs/box.lock`; inspect processes before and after acquisition. No parallel model calls
or sustained tests beside generation. Use subscription authentication only with the helper's
API-key environment removal. No direct API, fallback provider, reset or credit purchase.

Stop before another call once reported input plus output reaches 100000 tokens. An in-flight
call may cross the ceiling. Reasoning is part of output; cached input is part of input. Report
those components separately without double-counting. On a failed/incomplete slot, parser or
transport failure, or reported internal sampling retry, retain all artifacts and stop the
remaining slots without redraw. Never repair frozen code or prompts mid-run. Raw request,
stdout/stderr stream, result, retry diagnostics and incomplete slots remain visible.

## Reading and adverse findings

Read every complete raw result in full. Locate the opening and orientation; private scheme
and its three payment values; realization that sitting still pays nothing; withdrawal and
its physical timing; outline/register connection; cost of continuing to read; and first paid
movement at the endpoint. Separate the required understanding from subsequent paraphrases
of the same understanding. Inspect spatial clarity, object handling, sentence and paragraph
closures, repeated gestures, extra public rule announcements and unrelated exposition.

Protect the distinction between seeing the scheme and knowing WHY access occurs: the latter
is the separate `q_why_him` future reveal. Inspect all seven remaining hidden facts for early
answers: future headcount, reason for access, withdrawn-candidate destination, strike cost,
prior invigilator, ROOKE and transferred access. Required current events are not future-answer
leaks merely because a hidden compound claim mentions them. In particular, the hidden
headcount payload also contains the withdrawn student's name while the scene plan requires
that name. This separate conflict remains; name omission does not establish failure to
resolve the targeted `claim_edge` conflict.

Record missing/altered facts and values, causal/temporal confusion, identity/POV problems,
endpoint overshoot, length misses and any new defects. Retain all four slots with no ranking,
winner selection, repair or production acceptance. Shorter text or fewer explanations alone
does not establish better prose. Readings harvest located defects; they are not quality labels,
independent observations of a readership, or evidence qualifying an editorial mechanism.

The interpretation remains OBSERVED. Surviving redundant explanation rules out calling this
package a reliable cure on these draws. A pattern shared with controls is not credited to
permission. Even a located difference cannot establish a general effect or validate automatic
plan-to-permission inference. Report incomplete comparisons as incomplete, never as a null.

## Artifacts

Raw root: `runs/ab/prose-disclosure-permission-20260907`. Source and review tools:
`runs/ab/prose-disclosure-permission-tools`. Exact narrative text remains ignored. Commit
source-free execution/results and hashes, deviations and a scoped claim record; update
RESEARCH.md. Run repository handoff before registration and final commits.
