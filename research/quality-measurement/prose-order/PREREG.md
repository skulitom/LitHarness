# Discovery/action order diagnostic, 2026-09-06

CONJECTURE: requiring the first useful action to wait until after a witnessed death may
produce the long stationary interpretation interval located in the preceding trial. The
operator explicitly authorized this follow-up. It tests an event-order intervention, not a
quality metric, production gate, ranking mechanism or autonomous source-repair system.

## Inputs and conditions

Use the same original full neutral opening source and an occurrence-checked local amendment.
Retain the preceding trial's shared assumptions: starting endowment, movement eligibility,
personal count versus paper countdown, and in-person enforcement of withdrawal. Remove the
additional requirement that the first step occur at the endpoint. A priced attempted step
pays immediately whenever it occurs, including any additional qualifying attempts. Do not
hold a result back or silently lose extra earned marks to preserve the previous one-mark end.

Replace the ending instruction in both conditions with a common boundary: the required death,
outline, register lookup and first payment have occurred, with no later escape, purchase or
new rank, and the same unresolved questions. The final action need not be identical when
event order changes. Remove the previous intermediate beat list from both planning inputs.
All original source files, manuscripts, canon and production code remain untouched.

Before calling models, freeze an explicit outcome/dependency contract. Common dependencies
preserve light/paper/scheme timing, the priced actions, death before outline before lookup,
and adequate source-grounded information before relevant claims. Condition full requires
the first payment after death and lookup. Condition focused requires it before the death.
These IDs are cache names only: both receive the same full context. The narrative-specific
conditions are ignored fixture data; the runner validates generic dependency graphs.

Generate one fresh plan for each condition with identical planner instructions and source,
differing only in the declared dependency edges. The planner receives no previous plan,
draft prose, criticism, reader output or corpus material. It must construct attempts,
responses and consequences within the assigned order. This is a deliberate earlier-action
condition, not a test of whether merely granting permission spontaneously changes the order.

Read both complete proposals against source, including timer resets, payments, position-based
visibility, the compulsory victim's inaction and any information given to nearby students.
Record every source-only correction; never hand-rewrite a treatment to make it more dramatic.
Structural ID order is not semantic proof. An unresolved conflict, prohibited invention,
or wrong event order that requires redesign stops the paired draft phase. Preserve failures.
Freeze both reviewed plans and exact drafting requests before any prose call.

## Execution fixed before calls

Two planner calls in full/focused order, then four fresh drafts in full-1, focused-1,
focused-2, full-2 order. Maximum six invocations. Requested model gpt-6-astra, high reasoning,
through the signed-in Codex CLI and ChatGPT subscription quota only. Use the already tested
isolated transport with tools disabled and recognized startup-notice parser. No direct API,
API authentication, reset, fallback, retries or redraws after dispatch. Resolved model and
dollar cost remain unavailable when the envelope does not independently report them.

Before each call, stop at cumulative 140000 input + output + separately reported reasoning
tokens. Cached input is already within input. This is a dispatch stop, not an exact maximum
on the final call's output. The reused transport also has its own 120000 input/output stop
within each phase, which can only stop earlier. Missing usage, model/tool errors, changed
frozen bytes or unresolved source conflicts stop. Inspect processes and hold the shared
box lock for sustained checks and model calls, with one model arm and no heavy checks beside it.

## Reading and interpretation

Read all four entire scenes in generation order, retaining every output. Locate the first
attempt/payment relative to death and lookup; what the early action changes in subsequent
behavior; why the protagonist acts or delays; repeated explanation and introductory background;
invented mechanics, timing and source violations; extra earned marks and endpoint realization.
Do not score or rank the prose, count these as a craft metric, or select a preferred draft.

Earlier payment in treatment is manipulation delivery, not literary efficacy. The resulting
plans may differ in length, event content, viewpoint knowledge and closing action as well
as order; those consequences must be described, not attributed to one isolated cause.
Two draws per condition on one already studied premise cannot establish reliability, transfer
or a general cure for AI tells. Agent reading is defect harvest only. All prose and fixture
edits stay under ignored runs; commit numbers, identifiers, code and scoped findings.
