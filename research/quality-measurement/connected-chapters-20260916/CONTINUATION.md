# Explicit continuation after the registered token ceiling

The original run stopped at its registered ceiling with 77 completed calls and 2,536,927
recorded tokens. The last admitted call crossed 2,500,000, as permitted. All four books have
two accepted chapters, empty queues and no terminal jobs. A1, B1 and B2 completed grow2;
A2's grow2 attempt was refused before dispatch. Its recorded canonical metadata and job
counts are identical before and after that attempt. There is no failed/interrupted provider
call or unknown usage. No story prose, concept, outline or diagnostic narrative has been
read by the reviewing agent. Only operational metadata has been inspected.

The initial budget underestimated world-construction cost. The operator's requested
three-chapter demonstration remains incomplete. This explicit amendment finishes those same
four sequences. It changes only the aggregate token ceiling, to 3,500,000. Keep the original
140-call ceiling, 40 calls per book, 20 ticks per phase and 10,800-second clock starting at
the original dispatch, including this preparation gap. These are recorded subscription
resource limits, not dollar charges. No reset, purchase, fallback or model change.

## Preservation and remaining work

Before dispatch preserve the stopped progress record, every existing step and receipt, and
closed copies of all four databases under the ignored budget-stop directory. Freeze their
hashes with this continuation's source, tests and registration. Verify live stores equal
their closed snapshots before continuing. The same frozen production source, interpreters,
dependencies, native transport, concepts, outlines, author wording and accepted prose remain.
Do not re-invent, select, substitute, manually repair, revive a job or review narrative.

Repeat only A2 grow2 using the already registered logical ID scene-2. Preserve its earlier
refused step as grow2-A2-1; the resumed step gets iteration 2. Then run accept-grow2 for
all four books, chapter3 for all four, and drain3 for all four, in the original phase order.
Later steps also refuse an existing filename. Existing attempts count toward phase limits.
All ordinary policy attempts remain; any new stop ends this continuation without automatic
resumption. No more than three accepted chapters per book.

The first 77 receipt admissions are audited against the original token limit. Subsequent
admissions use the new aggregate limit, counting all original calls, tokens and elapsed time.
No attempt receipt or step is overwritten. The final report must retain the original partial
status and separate original and continuation usage. It is not an uninterrupted success of
the original registration. The existing descriptive readout, all-book reading requirement,
negative cases and no-quality/no-popularity-claim boundary remain unchanged.

Run the focused continuation checks and canonical handoff checker with billing disabled.
Prepare, validate, commit and push this amendment before dispatch. Hold the original owned
machine lock. The new driver refuses any state other than this exact budget stop and refuses
duplicate or implicit resumed dispatch. Read narrative only after this continuation finishes
or stops. Keep claims OBSERVED at most; no production source or default changes.

```powershell
uv run python tools/check.py handoff
uv run python research/quality-measurement/connected-chapters-20260916/continue.py prepare
uv run python research/quality-measurement/epistemic_governance.py research/quality-measurement/connected-chapters-20260916/continuation-claim.json
# Commit and push before:
uv run python research/quality-measurement/connected-chapters-20260916/continue.py run
uv run python research/quality-measurement/connected-chapters-20260916/continue.py audit
uv run python research/quality-measurement/connected-chapters-20260916/summarize.py
```
