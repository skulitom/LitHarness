# Test the four next priorities

The operator authorized these tests on 2026-09-13. This is an isolated engineering
and construction study. It does not qualify a reader or feed diagnostics into a
production book. Original stores, traces and accepted chapters remain unchanged.

## Frozen assignments

Use the completed `world-fixed-continuation-20260913` runtime and source without
production changes. Its manifest identifies the native Codex executable, dependency
inventory, subscription authentication and source hashes. Register the new scripts,
input transformations and source hashes before calls. Commit and push registration.
The concurrent precision patch is outside this source and this experiment.

1. **Goal boundary:** four fresh calls, in fixed order: current/conditional,
   clarified/adopted, current/adopted, clarified/conditional. Use the original
   Chapter 2 grow request, changing only the two located possibility paragraphs
   for the adopted case and appending one general instruction for the clarified
   case. Full prompts and the exact replacement stay under ignored `runs/`.
   Reconstruct four identical disposable worlds from the Chapter 1 accepted-record
   identities, resolving their full records and declaration times in the original
   read-only store. These are world fixtures with empty manuscript placeholders,
   not reconstructions of original jobs or history. Require their world readouts
   to equal the recorded checkpoint before calls. Record every resulting `wants`
   declaration and its source receipt; examine conditional and positive cases
   together. Only the positive case licenses saying the instruction did not simply
   suppress goal changes. One observation per cell establishes no general success
   rate or causal population effect. No automatic semantic scoring or goal repair.
2. **Progression:** take a SQLite backup of the completed three-chapter Wren store.
   Continue the existing plan through Chapters 4, 5 and 6 by ordinary tick, job
   draining, Architect grow, check and accept. No new outline, source rewriting,
   manual repair, state patch or diagnostic instruction is supplied. Preserve each
   accepted earlier chapter hash. Inspect planned versus actual rank, point and
   capability changes, including whether improvement can happen more than once.
   A stopped chapter remains a result; do not silently retry or extend the target.
3. **Reader-test construction:** use every retained private packet from the fixed
   September 13 admission cohort. Try removing the exact located payoff span and
   finding a non-overlapping sentence deletion with an identical shallow edit
   fingerprint, including position. Keep candidate texts private. A mechanically
   matched edit is still NOT an independently verified semantic damage/sham pair.
   Run an adversarial fixture where the uniquely located payoff has an additional
   differently worded realization. If deletion leaves that realization intact,
   exact-span admission alone cannot certify unpaid damage. No reader calls may
   be made from this construction study; it has no semantic oracle or holdout
   transformation implementations. Report feasibility and missing conditions.
4. **Architect cost:** derive per-call usage and query payloads from the earlier
   receipts and the new calls. Count repeated exact record payloads within each
   invocation, repeated queries, and static tool payloads. Separate payload bytes
   from native tokens; this descriptive profile cannot estimate causal savings.

## Bounds and controls

Hold `runs/box.lock` with the `next-priorities-20260913` owner while running sustained
checks or calls, and inspect processes as well as the lock. One sustained job at a
time. Aggregate live ceilings: 20 native attempts, 2,000,000 reported input plus
output tokens, and two hours for starting calls. The continuation's daily CLI
ceiling is 4,000,000 tokens to include its copied historical usage; the wrapper's
new-call aggregate ceiling remains authoritative. An in-flight call can overshoot.

Stop live work on transport/containment failure, unknown usage, changed source,
binary, scripts or dependencies, lost lock, changed protected original, attribution
failure, altered accepted chapter, or twelve ticks without expected progress.
Preserve failures and partial assignments. Do not implicitly resume. A semantic
goal outcome is an observation and does not by itself stop the other assignments.

The candidate clarification exists only in this experiment. Adopt no production
change on the basis of an unrun comparison. Mechanically correct progression and
completed chapters do not establish creativity, readership or literary quality.

## Execution

```powershell
runs/world-fixed-continuation-20260913/runtime/Scripts/python.exe research/quality-measurement/next-priorities-20260913/run.py prepare
uv run python tools/check.py handoff
# Commit and push only this experiment's registration, code and tests.
runs/world-fixed-continuation-20260913/runtime/Scripts/python.exe research/quality-measurement/next-priorities-20260913/run.py live
runs/world-fixed-continuation-20260913/runtime/Scripts/python.exe research/quality-measurement/next-priorities-20260913/probe.py
```

The preparation manifest records exact input, source, dependency and native binary
identities; `registration.json` addresses that manifest. Local receipts retain full
inputs, outputs, commands, usage, fixture identities and checkpoint views. Commit
only derived identifiers, numbers, hashes and scoped conclusions. Run handoff
before committing or handing off a completed change and validate the final claim.
