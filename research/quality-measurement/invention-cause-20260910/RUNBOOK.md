# Invention repetition: staged causal debugging

The operator authorized improving debugging tools and using them until the repeated aquatic
repair premise is explained. This is authoring-process diagnosis, not a literary-quality
measure, a reader qualification, or candidate selection. The previous explanation about
constraint-heavy wording remains a conjecture. The historical search already finds aquatic
material in older Codex profiles, so the latest discovery revision is not its sole origin.

## Phase 1: full versus minimal invention

Use the baseline's frozen production source at d5ccb9ec7a244e8a573a0c26612373672d3c2bd7,
its isolated interpreter, Codex gpt-6-astra at medium reasoning, and the current native binary
with its identity frozen before dispatch. No writer, shelf, previous concept, database,
diagnostic text or corpus enters generation. No tool is allowed in these completions.

Full is the exact production discovery request with an empty brief and third-person narration.
Minimal replaces only its system string with:

> Invent an original LitRPG story in portal fantasy, isekai, or system apocalypse. Describe its
> world, the opening chapter's events, and the protagonist's possible growth in the three
> requested fields. Return story material rather than advice.

Keep the user prompt, native/application schema, 2,400-token requested output allowance,
600-second timeout and all transport settings identical. No seed or temperature is supplied;
the native interface does not report its resolved sampling distribution. Model identity is
requested, not independently confirmed by Codex's event stream.

Freeze eight independent first-response requests before calls, in order:
full-1, minimal-1, minimal-2, full-2, minimal-3, full-3, full-4, minimal-4.
Every call gets a fresh ephemeral native session. Validate with Discovery.from_invention,
retain failures, and never redraw or modify a returned story. There are no downstream concept,
listing, drafting, world-building or reader calls. Do not inspect story outputs until all
eight slots finish or an operational stopping condition prevents continuation.

Bound this phase to eight completion attempts and 160,000 returned tokens, including failed
responses where recoverable. Check totals before each new call; a native in-flight response
can overshoot the limit. A transport/auth/quota failure, unknown usage or frozen-input drift
stops remaining calls. No provider fallback, usage reset, API spend or live-provider test flag.

## Inspection and next branch

Use tools/generation_trace.py to verify application and native input differences, distinct
session IDs, retained outputs and source hashes. Search water-setting terms, repair activities
and recurring names with field locations, then read every complete response. Search hits
locate text; counts are not a quality score or a semantic classifier. Water in an incidental
sentence is not equivalent to a water-world premise. Preserve counterexamples and mirrors.

If the specific repeated premise persists under minimal, the extra production prose is not
necessary for it on these draws; investigate model/transport and task framing next. If minimal
breaks the cluster while full retains it, isolate the contributing prompt blocks with matched
deletions and add-back controls. Mixed results need more controlled inspection, not a forced
verdict. No result here alone identifies a particular sentence or proves a general distribution.

Any next phase requires a new frozen schedule, named manipulation and controls committed
before its calls. Do not change this phase after inspecting outputs. Stop claiming a cause
where evidence only locates a stage. No aquatic blacklist or production prompt change follows
automatically from this diagnostic.

## Execution and retention

Read CONTRIBUTING.md, the quality-measurement BRIEF and EPISTEMIC_GOVERNANCE, and the shared
RUNBOOK's box procedure. Acquire runs/box.lock after checking processes. Run one sustained
job at a time. The trace tool never opens a book store or provider. Keep full prompts, traces,
output prose and operational logs under ignored runs/invention-cause-20260910/. Commit only
code, registration, hashes, derived records and bounded conclusions. Preserve unrelated edits.

Run run.py prepare with the baseline's isolated interpreter, perform focused tests and handoff,
and commit this registration and the prepared manifest's hashes before run.py run. The runner
refuses implicit resumes. Each registered phase remains in the record even if inconclusive.
