# Scene choices, competing interests and explanation: one opening trial

The operator asked to try fixing the six-chapter opening's repetitive procedure,
weak Load/Duration trade-off, narrow character pressure and repeated qualifications.
Those editorial observations motivate this experiment; they are not reader labels
or validated quality measurements. Preserve the earlier opening as a historical
comparison. Generate one new opening from the same first retained seed and concept,
with the same six-scene layout, 1,800-word target, normal production acceptance,
reconciliation, default native model/reasoning and verbatim tool replies.

Two explicit source overrides on base `52c2fdf` constitute the trial:

- `application/outline.py`: concept planning moves to `planner.outline.v2`. Its
  handoff develops consequential choices with their motives, forgone alternatives
  and later effects, retains other participants' aims, and distinguishes a changed
  situation from another demonstration with different materials. It does not impose
  mistakes, losses or antagonism where the story does not call for them. Legacy
  non-concept planning remains v1 and author locks keep precedence.
- `domain/house.py`: named-viewpoint drafting adds one explanation-limit instruction.
  The character's knowledge limits remain, while conduct and events can carry
  distinctions already clear. Other roles' general clarity floor remains unchanged.

The directions are bundled in this pilot. A changed output will not identify which
direction caused it or establish a general improvement. No author note retells the
old chapters, no old chapter is revised, no diagnostic enters the production loop,
and no rule is relaxed to purchase an intended outcome. The original concept remains
generated, revisable material: its directions do not become new author locks or
mandatory chapter deadlines. Do not force repeated Load investment or a particular
side-character conflict to make the readout look successful.

## Registration and execution

The runner reuses the preceding frozen continuation helpers in private module
instances. The archive comes from `52c2fdf`; only the two named files are overlaid
before importing the package. Commit and push these exact files, scripts, tests and
registration before the first call. Hash the source tree, scripts, native binary,
dependencies, original seed, initial reconstruction, concept and preceding book
receipts/chapters. Unrelated working-tree changes are excluded. Freeze the script
chain as well as the top-level wrapper; never edit a prior registered file.

Take `runs/box.lock` with owner prefix `scene-consequence-20260913:` after checking
processes. One sustained job at a time. Use the registered **32 native attempts,
2,200,000 reported tokens and 90 minutes for starting calls**. In-flight calls can
overshoot. Health and all sub-calls count. Retain every first result and failure;
no retries of failed native calls, fallback, reroll, implicit resume, ceiling increase
or extension beyond **six chapters**. No reader/evaluator calls or candidate selection.

Stop on source/input/runtime/binary drift, lost lock, transport or containment
failure, unknown/mismatched usage, world-check or attribution failure, changed earlier
accepted text, too many chapters, or lack of expected progress within twelve ticks.
Each accepted chapter receives an immutable database backup and saved checks, state,
plan inventory, diagnostic audit and chapter hashes. Original book/seed remain
unchanged; partial runs remain results.

## Readout fixed before calls

Verify provenance, bounded transport, actual outline/writer instruction delivery,
chapter attribution, earlier-text preservation and seed mechanics. Reuse the original
audit plus the supersession-aware reader: the historical all-seed-canon flag remains
visible, while the active test requires all original rows with the authority implied
by original declaration order. Keep partial status snapshots, missing typed sheets,
default stock zero and descriptive audit exit codes explicit. Request phrase locators
in the reused reader concern the old growth phrases; the wrapper separately checks
actual v2 planner and named-viewpoint writer contracts.

Read every new chapter in full, then locate these observations against actual plans,
requests, prose and accepted state:

1. Does the intended Load/Duration dissatisfaction affect later action, or is the
   choice immediately vindicated/compensated? Keep useful Load effects and ordinary
   mana expenditure distinct from opportunity cost. Absence within six chapters is
   unobserved delivery, not permission to prolong the run.
2. Which exchanges contain a participant's understandable aim that remains relevant
   after the technical facts are established? Agreement is not itself a defect.
3. Where do Wren's concerns or responses affect a choice beyond naming fatigue or
   fear? Preserve contrary examples and any new implausible behavior.
4. Where does another technical procedure change the situation, and where does the
   text explain a distinction already carried by action? Locate passages; do not
   turn sentence forms, procedure counts, reduced length or lexical absence into
   quality scores.
5. Do the expertise, system limits, memorial relationship, permanent-repair discovery
   and unknown-marker boundaries survive? A different plot can still lose these.

This reading is an editorial assessment of one first output. Tests establish prompt
routing and mechanics, not literary efficacy. No model ranks candidates, no finding
becomes automatic editorial direction, and no reader mechanism gains qualification.
Report remaining defects and new failures. Keep raw prose and traces under ignored
`runs/scene-consequence-20260913/`; commit derived numbers, identifiers and hashes.

```powershell
uv run python research/quality-measurement/scene-consequence-20260913/run.py prepare
uv run python tools/check.py handoff
# Commit and push registration and source overrides before generation.
runs/scene-consequence-20260913/runtime/Scripts/python.exe research/quality-measurement/scene-consequence-20260913/run.py live
runs/scene-consequence-20260913/runtime/Scripts/python.exe research/quality-measurement/scene-consequence-20260913/audit.py
```

Run handoff before the result commit; release only this task's lock afterward.
