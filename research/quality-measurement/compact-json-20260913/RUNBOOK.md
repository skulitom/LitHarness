# Compact JSON replies: fixed four-cell comparison

The operator authorized autonomous continuation while away. The preceding
[payload measurement](../growth-declarations-20260913/REPORT.md) found a lossless
byte reduction, not a model-token or behavior result. This comparison tests the
opt-in implementation at `991ff2c`; the production default remains verbatim.

## Frozen cells and measurement

Use the preceding study's exact conditional and adopted Chapter 2 grow v5 requests.
Restore four identical Chapter 1 world fixtures from the original recorded identities
and declaration times. Preserve their complete readout; no manuscript is generated,
world proposal accepted, source edited or diagnostic feedback inserted.

Run one fresh call per cell in this fixed order:

1. Verbatim / conditional.
2. Compact / adopted.
3. Compact / conditional.
4. Verbatim / adopted.

Each pair has byte-identical application requests and initial world readouts. Both
formats use the same frozen production source, native binary, model and restricted
bridge. Only `CodexCliProvider.compact_tool_json` changes. Compaction removes whitespace
outside JSON strings in successful object/array stdout. Numbers, escaped strings,
duplicate keys and all other characters remain exact; raw CLI receipts, stderr,
non-JSON output and failure replies are preserved. No format label reaches the model.

For each cell record all returned wants assertions, scene keys, world checks, tool
receipts, command counts and fresh native session identities. Conditional output must
not promote the possibility into a homeward pursuit; adopted output must retain the
explicit pursuit. A complete command trace must show exact original-versus-visible
stdout correspondence and the selected transport format. Retain all differing goals
and commands, not just the target phrase. Treat semantic or coherence failure as a
result, not a reason to retry; continue the independent fixed cells when containment
and provenance remain valid.

Compare reported input plus output tokens, cached input, output tokens, elapsed time,
visible stdout bytes and command counts within each conditional/adopted pair. Health
usage counts against the aggregate ceiling but is reported separately. Dynamic query
and declaration differences, cache state and one observation per cell limit attribution:
the smaller byte format does not predetermine lower native tokens or elapsed time.
Report both pairs even if directions disagree. Do not change the default during the
study, qualify a reader mechanism or infer literary quality.

## Bounds and guards

Eight native attempts including health probes, 1,200,000 reported input plus output
tokens and 60 minutes for starting calls are aggregate ceilings. An in-flight call can
overshoot. Freeze and hash source, migrations, lockfile, dependency inventory, the prior
pinned native executable, requests, fixture stores and every imported experiment helper.
Commit and push registration, scripts and tests before calls. Use subscription auth,
fresh native sessions and the restricted production bridge without fallback.

Hold `runs/box.lock` with owner `compact-json-20260913:` for sustained work. Inspect
processes as well as the lock. Never overlap another session's sustained work, and do
not run validation beside calls. Stop live work on containment/transport failure,
unknown usage, changed source/binary/dependencies/scripts/protected inputs, lost lock,
fixture mismatch or missing attribution. Preserve partial results without implicit
resumption. Raw book text and responses remain ignored under `runs/compact-json-20260913/`.

## Execution and analysis

The runner reuses the preceding frozen driver through an explicit private module and
overrides only the named experiment constants. It creates new fixtures and wraps the
provider solely to select the registered reply format before each call. Both the wrapper
and imported driver are hashed. Ten previous guard regressions remain unchanged.

```powershell
uv run python research/quality-measurement/compact-json-20260913/run.py prepare
uv run python tools/check.py handoff
# Commit and push the frozen registration, scripts and tests.
runs/compact-json-20260913/runtime/Scripts/python.exe research/quality-measurement/compact-json-20260913/run.py live
runs/compact-json-20260913/runtime/Scripts/python.exe research/quality-measurement/compact-json-20260913/audit.py
```

`audit.py` rebuilds command byte counts, format correspondence, native usage, goal
locators and paired differences. Commit hashes, identifiers, derived numbers and scoped
conclusions. Run handoff before committing or handing off the completed result. Follow-up
implementation decisions must acknowledge the fixed-pair limits and any failed control.
