# Bounded retrieval and usable timing, with a separate completion fault exposed

**State: OBSERVED.** Both assigned cases completed with every first output retained.
All source/provenance/containment controls passed and the earlier databases and chapter
exports are unchanged. The frozen source is `91271ac`; registration was pushed as
`f7623ec` before dispatch. Changes made after that snapshot did not enter this run.

The run used nine native calls and 628,443 reported tokens over 969.9 seconds. Wren used
seven calls / 453,529 tokens; the opposing source used two / 174,914. Requested model and
reasoning remain the repository defaults under the registered subscription CLI, without
independent backend resolution. This is another execution of fixed sources, with fresh
stores and sessions; it does not invent or select a new story.

## Retrieval and timing

No Architect attempted or executed `world show`. All thirteen queries succeeded and
returned at most twenty records. Wren seed returned 8,216 query bytes, reconciliation
75,852, and opposing seed 5,830. Reconciliation traversed all 175 in-force records over
nine pages with one stable selection hash, repeated its first page once, then requested
a scoped page. Pagination worked, but the model still scanned the whole world and the
repeated first page remains visible. Record caps do not impose a byte cap on long values.

Reconciliation received `s000001` explicitly. All 53 newly accepted facts use that key;
no accepted fact has an unplaceable key. Every saved seed/grow check has an empty
unplaceable list. The final world check has no gaps, snapshot faults or completion
complaints, and verification reconstructs both revisions with no attribution gaps.
This contrasts with the preceding run's twenty accepted facts at unusable schedule keys.

Reconciliation used 153,767 reported tokens, versus 278,735 in the preceding run. These
are sequential first outputs with different world declarations, not a causal savings
estimate. World work still accounts for 568,524 of this run's reported tokens.

## Source and chapter observations

Wren's development retains survival, structural exploration and the false-condemnation
mystery without adding a return-home pursuit. The seed retains her existing inspection
expertise but leaves Loadstitch unowned and the assessment pending. It adds no Mara or
exclusive fork. The opposing seed retains Mara, her return-home motive, Rank Two, owned
Fold and Anchor, the pending Step acquisition with its two-trial quota, and the permanent
Mirror/Echo choice. Additional prerequisite depths are a separate defect below.

The complete new Wren chapter was read: 1,966 whitespace words, SHA-256
`f73fd1a06db63d013c85afe33fe407f3fed419ad9edcf49aa29bb0cb50eca57e`.
In the reading copy under `runs/world-runtime-fixes-20260913/book-1/library/`, the assessment
appears at line 61; a rejected prediction at 91; completion and Loadstitch at 101–103;
the restrained-load crossing at 155; the rank/point reward at 165; and the unexplained
condemnation at 201. The chapter carries the located acquisition sequence onto the page.
This is diagnostic reading, not an audience label or a qualified Mother of Learning comparison.

## Completion pressure to invent depth

Wren seed call `003`, bridge result 8, initially declares Split and Articulated as requiring
Loadstitch ownership plus the supplied rank gates. Check 9 refuses to finish the system
because nothing exceeds depth one. Batch 10 then raises those requirements to five and
ten and equates Loadstitch depth with rank. Check 14 exposes another implementation
restriction: the system name is too long for a configuration scale label, although that
label never occupies a printed status column.

The opposing seed call `002`, check 7, has the same no-depth refusal. Its final accepted
world adds Fold depth two as a prerequisite for Mirror and Echo, beyond the fixed source's
rank/fork conditions. Preservation of the initial ownership and motives therefore does
not mean all access conditions survived unchanged.

The next engineering change accepts ownership-only systems, separates rank gates from
depth, and applies printed-label restrictions only to fields actually printed. The
[deterministic replay](../world-depth-fixes-20260913/RUNBOOK.md) tests the captured pre-check
declarations in both versions. It neither changes these retained outputs nor supplies
diagnostic prose to generation.

## Validation

The engineering snapshot passed repository handoff checks: 5,028 tests and 20 skips,
89.84% coverage, lint, types, wheel build and corpus-history audit. Added tests cover
pagination, retained provenance, refusal of full-history access before execution, invalid
key rejection before identity insertion, and the actual serial/legacy scene coordinates.
`audit.py` rebuilds the derived record from retained receipts and checkpoints. System voice
remains unchecked as a quality mechanism; no reader mechanism is qualified by this run.
