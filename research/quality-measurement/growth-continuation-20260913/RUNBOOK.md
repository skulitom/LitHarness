# Six chapters from the retained growth-declaration seed

The operator asked to proceed with growth integration. The deterministic test in
`tests/test_growth_integration.py` follows a scripted second paid investment through
the real selector, writer request, draft acceptance, extraction and next request.
It checks open growth and an explicit cap, without a model or a story-quality claim.

This live continuation uses the preceding study's first Wren seed, at
`runs/growth-declarations-20260913/wren-seed/serial.db`, and its unchanged original
concept. It does not rerun or choose among seeds. Reconstruct that complete seed
world with its original record identities and declaration times in a new six-scene
book, then use ordinary check/accept, planning and ticks. The original seed remains
unchanged. The new book has one scene per chapter, six chapters per arc, third-person
concept planning and a target of 1,800 words per chapter. The neutral title and premise
are fixed in the runner before generation. No source chapter enters the new book.

Production source is frozen at `23243c3`, including seed v8, grow v5 and verbatim
Codex tool replies. Use the same pinned subscription-authenticated native executable,
model/reasoning defaults and restricted bridge as the preceding study. No provider
fallback, new invention, manual plan edit, manuscript repair or diagnostic feedback
is permitted. No goal, request or schedule requires repeated purchase. Its absence
after the fixed six chapters is a result, not permission to extend the run.

## Bounds and stops

Register, commit and push source/script/input hashes, native binary, dependencies,
concept and initial fixture before the first call. The driver imports only explicitly
hashed earlier helpers. Every native call, including health and any ordinary pipeline
sub-call, counts toward **32 attempts, 2,200,000 reported input plus output tokens,
and 90 minutes for starting calls**. In-flight calls may overshoot. Run exactly six
chapters or stop earlier. Retain every first output and failure; never implicitly
retry a failed command, resume a stopped run or raise the ceilings.

Hold `runs/box.lock` with owner `growth-continuation-20260913:` for sustained work.
Inspect processes as well as the lock. Never overlap calls with another sustained
job or repository checks. Stop on lost lock, source/binary/dependency/script/input
drift, transport or containment failure, unknown or mismatched usage, failing world
check or attribution, changed prior accepted chapter, excess chapter count, or no
expected progress after twelve ticks. Preserve partial receipts and stores on stop.

After each accepted chapter, drain only queued non-draft work, run grow/check/accept,
and save readouts plus a read-only database backup. These are separate immutable
checkpoints. Reconciliation remains the production workflow; this experiment does
not add assertions based on its own reading.

## Measurements and limits

Audit exact original-seed reconstruction and subsequent accepted seed identities,
native sessions, usage by profile, bound tool commands and reply format. Save each
checkpoint's world check, revision attribution, chapter hashes and complete state.
Read historical sheets from each checkpoint's own database, retaining all system
caps, holdings, stock and rank values. Record transitions and point arithmetic under
the declared seed model; do not assume any increase is a valid purchase merely
because it appears in a status line. Preserve missing sheets and failed arithmetic.

Read every new chapter in full. Locate acquisition, rank movement, repeatable
investment and point expenditure against the source, accepted state and actual
writer requests. Record new goals and their source passages, including any renewed
conditional-to-adopted drift. Whole output differences remain observations, not
causal model-format effects or a complete semantic-compliance guarantee.

Success means six accepted chapters with attributable revisions, preserved earlier
text and seed rules, passing checks and bounded transport. A repeated investment,
if it occurs, must be examined separately for its price and downstream state. Its
absence does not refute support for repeatable growth, but leaves that live boundary
untested. No reader mechanism, quality score or editorial qualification follows.

```powershell
uv run python research/quality-measurement/growth-continuation-20260913/run.py prepare
uv run python tools/check.py handoff
# Commit and push the registration before generation.
runs/growth-continuation-20260913/runtime/Scripts/python.exe research/quality-measurement/growth-continuation-20260913/run.py live
runs/growth-continuation-20260913/runtime/Scripts/python.exe research/quality-measurement/growth-continuation-20260913/audit.py
```

Raw evidence stays under ignored `runs/growth-continuation-20260913/`. Commit only
derived numbers, identifiers, hashes and scoped conclusions. Run handoff before the
result commit and release only this task's lock.
