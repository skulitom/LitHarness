# Duplicate payoff instructions in the original production scene request

2026-09-07. REGISTERED only after commit, before any new generation. This is a bounded
diagnostic of one production scene fixture. No literary quality scale or production
authority is established by agent readings.

## Question and fixed change

The recovered original scene-one request repeats four payoff bodies in its planned-story
debt list and open-thread ledger. Does removing the first copy change redundant explanation
or premature settling of meaning in fresh realizations of that scene? Model style, the
other instructions, information density and source mechanics remain competing explanations.

Control is the exact flat request-4.json from runs/ab/chapter-rule-context-20260905, SHA-256
4cb3c1292b764ed9087a43fbbd903f9848688a16e1ec3bf15b2d23802722b13f. Its system and prompt hashes
also match the scene_trace view of the retained database. This uses the original knowledge
headings, not the two later corrections in the reviewed-input manifest. The original
request has a 900-word scene target and timeout 300, and generated only the first of two
chapter-one scenes. It is not a whole-chapter reconstruction or a new second-scene context.

The single-copy treatment deletes exactly four reviewed `: BODY` spans from the first debt
list. Each unique question/title and its by-scene deadline remains. Every deleted payoff
body occurs verbatim in the untouched open-thread section, whose exact pay-within windows
also remain. No qualifier is selectively rewritten; no story fact or unique timing rule
is removed. System, writer dossier, house instructions, all other prompt text, scene plan,
viewpoint, required displays, endpoint, rules, hidden truths and word target are unchanged.
The ignored source file records exact character offsets, deleted text and surviving witnesses.
This tests a duplication-removal package: length, frequency and placement cannot be separately
attributed. It is not deletion of all explanatory duties or of the required character insight.

## Dispatch

Four fresh independent scenes, in fixed order: control-1, single-1, single-2, control-2.
Two draws per condition; no ranking, winner selection, repair or production acceptance.
Original manuscript and historical output are not shown to the writers. Only the original
production request and the registered cuts enter them; the fixture contains no exemplar shelf.

Use the existing signed-in Codex CLI helper, requested gpt-6-astra/high, with the same isolated
settings in all slots. Historical generation used Claude Code, so the fresh control, not
historical prose, is the transport-matched comparator. Full upstream instructions and resolved
model are not captured. The helper's subprocess timeout is 900 seconds; source timeout 300
is retained as provenance, not claimed to control the new subprocess.

Freeze the code, reviewed source, original input, registration, system files and transport
identities before dispatch. Hold runs/box.lock; no parallel model calls or sustained checks.
Use subscription authentication only; remove API-key environment variables through the
existing helper. No API generation, alternative provider, reset or purchase.

Stop before another call once cumulative reported input plus output reaches 100000 tokens.
Reasoning is a component of output and cached input a component of input; report both
without adding them again. An in-flight call may cross the ceiling. Retain every request,
raw stream, result, failure and incomplete slot. On transport/parser failure or internal
sampling retry reported in stderr, retain the result and stop subsequent slots without
redraw. A retained retried output does not become a clean replicate. Do not repair prompts
or frozen code after any slot begins.

## Reading and adverse findings

Read every complete output as raw provider-adapter text, without cleanup or revising it.
The same deterministic production cleanup may be displayed separately for inspection,
clearly labelled; never substitute it for the registered raw comparison.

Locate the opening and orientation; the private scheme and its three item values; recognition
that sitting still pays nothing; the withdrawal and its physical sequence; the outline/name
connection; recognition of the cost of continuing to read; and the first paid movement at
the endpoint. For each interpretation, distinguish the required understanding from a second
paraphrase after it is already clear. Inspect object handling and spatial clarity, exposition
of unrelated world material, repeated gestures, premature answers to future debts, and any
other defects across the entire scene. Do not require future facts to appear in scene one.

Record missing or altered facts, numbers, identity/POV errors, causal or temporal confusion,
source contradictions, endpoint overshoot, degraded clarity and length misses. A shorter
output, fewer explanations or fewer instructions is not itself a quality improvement.
Readings are located defect harvest, not labels, independent units, or a mechanism qualification.
All four outputs must remain visible. The interpretation stays OBSERVED: two draws of one
fixture do not isolate a general effect, and a surviving defect blocks any claim that this
deletion reliably cures it here. A treatment absent in the control is not credited to it.

## Retention

Ignored raw root: runs/ab/prose-production-obligations-20260907. Ignored source/audit root:
runs/ab/prose-production-obligations-tools. Commit source-free results, provenance hashes,
deviations and claim updates; update RESEARCH.md visibly. No current production prompt or
manuscript is changed by this experiment. Run handoff before registration and final commit.
