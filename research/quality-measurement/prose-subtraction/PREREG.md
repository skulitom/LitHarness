# Deletion-only diagnostic, 2026-09-05

Status: CONJECTURE; an operator-authorized local editing experiment, not a qualified reader
mechanism, ranking, quality metric or production revision. Read the parent BRIEF.md and
EPISTEMIC_GOVERNANCE.md. The full-context/reconstruction trial has completed eleven calls.
Inspection located repeated explanations and revived comparisons in both reconstructions.
Those readings harvest defects, not evidence that this treatment will work.

Freeze two sources from `runs/ab/prose-reconstruction-20260905`: the existing criticized
Chapter 1 in its manifest, then `literal_unbounded-1.result.json`. These are the same fixed
substrates used for reconstruction, not winning candidates. No new source sampling.

One claude-opus-5 call per source returns exact spans to delete and local reasons. Scope is
repeated explanation, interrupting comparisons and commentary announcing meaning or future
recollection. Preserve events, causal relations, choices, uncertainty, knowledge boundaries,
consequential quantities, printed text and dialogue. Incidental emphasis and redundant
interpretation may be removed. There is no deletion quota, word target or preferred example.
No additions or replacement prose are possible. Retain empty or invalid answers; no redraw.

The applier validates source identity, unique matches (including overlapping occurrences),
paragraph bounds and nonoverlapping spans. It removes those spans in reverse source order and
normalizes paragraph-edge whitespace. These controls do not prove grammar, semantic fidelity,
or improved prose. Read both complete results and every proposed cut against its source.
Report removed information, damaged grammar and surviving problems as well as visible cuts.
No model rates, ranks or selects outputs. No result automatically changes production.

Two calls maximum, sequential, no retries or health calls. Stop before another call once
reported equivalent cost reaches $2.00 or cost is missing; an in-flight call can exceed it.
Use the existing request/result recorder and its test billing guard. Freeze source/code hashes
before calls. Commit this registration and call-free controls first. Keep prose under ignored
`runs/ab/`, and commit only derived numbers, identifiers, code and scoped findings.
