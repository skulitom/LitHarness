# Carry the first crossed plan pair through one common chapter writer

The operator authorized the proposed downstream transfer comparison on September 13, 2026.
This isolated two-call experiment changes no production default, accepted book, selector or
quality mechanism. The previous plans are known cases; only the new chapter outputs are unseen.
The claim stays OBSERVED. There is no provider ranking, creativity score or audience verdict.

## Fixed inputs and contrast

Select the first mechanics design in the previous registered dispatch order, magic-astra-1,
and both of its expansions: plan-astra-astra-1 and plan-astra-opus-1 under
`runs/invention-boundaries-20260912/calls`. This ordinal rule retains the pair regardless of
known strengths or defects; it is not selection of the most promising plan. Both use Loadstitch.
Verify receipts against the previous committed evidence inventory, verify their identical
mechanics lineage, and rebuild their discovery request from that mechanics object and seed.

Draft first from the Astra plan, then the Opus plan. Give each complete, unedited plan to the
unchanged `draft_request` from mechanics-fresh-transfer-20260912. It asks for 1200-1600 words
of third-person, past-tense finished fiction, dramatizing opening with world/growth as background.
The plan's provider name, observer notes, known defects, other plan and raw mechanics object
are not supplied separately. There is no corrective instruction, anti-repetition list or repair.

Draw one new 2048-bit random integer and encode through unchanged invention-seed.v3. Use that
same Base64 preprompt for both chapters, with gpt-6-astra, medium effort, the same story-string
schema, 4200 requested output tokens and 600-second timeout. Native sessions are distinct,
ephemeral and use empty working directories, disabled tools, project documents and memory.

The old 7ac07f4ce733f89a CLI binary is no longer present. Use and freeze the currently installed
bffc5354119c8421 binary for both calls. The requested writer and task match the prior work;
the changed launcher and hidden backend prevent claiming an exact reproduction across runs.
The two complete source plans have different lengths (796 and 2002 words). Keep that difference
visible: this tests transfer from these plans, not a length-controlled provider effect.

## Reading and interpretation fixed before calls

Read both complete chapters only after both finish or the run stops. Locate source relations
by paragraph, distinguishing enactment, compression, missing coverage, contradiction and newly
added dependencies. A future growth event need not occur in the opening; repair appearing in
either chapter is already compatible with its source.

For the Astra plan, inspect Mara's pursuit of her separated mother, the twenty-joint assessment,
the rotten-handle failure despite an intact seam, the sound replacement operating the sluice,
contact with the mother, and inspection-for-passage leading toward the route home.

For the Opus plan, inspect Wren's thirst, inspection and the assessment, rejecting a seam that
would carry her whole weight, using bearing support with a seam resisting sideways slip,
getting herself and the chalkrat across, reaching water, and finding the Earth-marked false
condemnation. Inspect whether the continuing pursuit remains the supplied mystery or whether
the writer introduces a family-rescue/repair-for-home dependency absent from this opening.

For both, trace observable uses of Loadstitch: contact, nonliving solids, no material
strengthening, one initial seam, 100 N load, ten-centimetre length, sixty-second duration and
five-mana cost. Report whether a limit matters to a choice or failure, is merely restated,
is contradicted, or cannot be assessed from what is shown. Do not require later powers to
appear or equate the number of displayed rules with quality.

The Opus plan already describes two wrong predictions among twenty assessed joints without
describing successful reassessment. Record what the chapter does with that known gap rather
than silently repairing the input or attributing the gap wholly to drafting. Its seam timing
and stated load exposure also need reading rather than an assumption of correctness.

If the different immediate and continuing pursuits survive, retain a narrow transfer observation;
this would not demonstrate independent invention or broad diversity. If the Opus-derived
chapter replaces its supplied pursuit with the old pattern, locate the first added dependency
and treat drafting/common context as a candidate influence. Missing later coverage is inconclusive.
If both collapse, report both. No single outcome licenses a default switch or quality claim.

## Operations and evidence

Two maximum application calls, sequential; no new call after 30000 recorded tokens. The
ceiling is checked before dispatch, so the final call may exceed it. Keep every first result.
No retry, fallback, substitution, quota reset or implicit resume. Transport error, unknown
usage, drift, invalid designated parent or lock loss stops execution. Native internal turns
are recorded and are not application retries. No partial-result reading changes the second call.

Use the shared-machine lock and one test/math worker. Freeze sources, extracted plans, seed,
requests, production source, utility code, runner, tests, RUNBOOK and binary. Commit and push
the registration after handoff validation and before any generation. Reuse the previous
sequential execution loop with explicitly bound paths and slots; do not modify its frozen file.
Save exact requests, started/completed/failed receipts, usage and native traces atomically.

Audit frozen hashes, request reconstruction, shared settings, lineage, captured transport and
fresh session identities. Raw text stays in ignored `runs/planner-chapter-transfer-20260913`;
commit derived observations, hashes and identifiers. Validate claim pointers and rebuild the
audit byte-identically before pushing results. Release only the owned lock.

```powershell
uv run python research/quality-measurement/planner-chapter-transfer-20260913/run.py prepare
uv run python tools/check.py handoff
# Commit and push registration before dispatch.
uv run python research/quality-measurement/planner-chapter-transfer-20260913/run.py run
uv run python research/quality-measurement/planner-chapter-transfer-20260913/run.py audit
```
