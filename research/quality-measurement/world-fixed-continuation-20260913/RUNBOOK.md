# Fixed story continuation after world-completion repairs

## Question and assignments

The preceding [completion replay](../world-depth-fixes-20260913/REPORT.md) removed
the mandatory numerical-depth and unprinted configuration-label refusals for the
captured Wren world. Its opposing snapshot remained incomplete. This run checks
fresh generation with those changes, then the operator's selected milestone: one
fixed story through three chapters of the ordinary pipeline.

Assignment one is the original Wren treatment at
`runs/invention-boundaries-20260912/calls/plan-astra-opus-1.json`, using only
`result.parsed`. Assignment two is the unchanged opposing treatment at
`runs/world-boundary-fixes-20260913/control-treatment.json`, through seed only.
Their SHA-256 values are registered before calls. Order is Wren, then opposing.
Retain each first output, including any failure; neither case is replaced or
selected after viewing its output. This is fresh execution of fixed treatments,
not a fresh invention or a reader comparison.

The first case checks acquisition after the opening assessment, existing mundane
expertise, supplied rank gates, repeatable numerical point allocation, and jointly
attainable later capabilities. The opposing case checks already-owned Fold and
Anchor, the supplied home motive, two new trials before Step, and the explicitly
permanent Mirror/Echo choice. Source-preservation judgments must cite actual
requests, commands and resulting records, not the Architect's account of its work.

## Frozen execution

This directory's driver adapts the preceding boundary capture driver without
editing earlier registrations. Preparation archives committed `src`, migrations,
project metadata and lockfile; freezes the interpreter's dependency inventory;
and hashes the driver, auditors, this runbook and native executable. The manifest
also protects the prior three-chapter, boundary and runtime stores and chapters.
Uncommitted precision work is excluded from the production snapshot.

Use Codex CLI `0.154.0-alpha.6.2`, ChatGPT subscription authentication, and the
repository's default model/reasoning. Preserve strict tool containment and
existing native timeouts. There is no API transport or provider fallback.
Commit and push the prepared registration before the first model call.

Each source receives one normal concept-development call. Use ordinary `new
--concept`, the preceding fixed neutral title/premise, six scenes, third person,
one scene per chapter, six chapters per arc and 1,800 target words. Then run
seed/check/accept. Wren continues through normal ticks, follow-up job draining,
grow/check/accept, and a saved checkpoint after each of three accepted chapters.
The opposing case ends after seed/check/accept. Save empty, seeded, per-chapter
and final audit/status/jobs/plans/state/verify/world snapshots and chapter hashes.

No new invention, precision edit, candidate ranking, manual manuscript repair,
direct plan/state mutation or imported chapter is used. Diagnostic observations,
earlier chapters and suspected defects do not enter generation. Any subsequent
repair or replay must preserve these first outputs and identify itself separately.

## Bounds and stops

One sustained job runs under this task's `world-fixed-continuation-20260913`
box-lock prefix. Ceilings are 40 native attempts and 2,200,000 reported tokens in
aggregate, 35 attempts and 1,900,000 tokens per book, and two hours for starting
new calls. These ceilings allow the earlier measured three-chapter cost plus the
opposing seed. Checks occur before a call; an in-flight call may overshoot.

Stop the whole run on source, script, binary or dependency drift; unknown native
usage; transport or containment failure; failed attribution; altered earlier
accepted chapter; unexpected chapter count; or twelve ticks without the expected
progress. Do not implicitly resume or retry a stopped book. Retain partial stores,
all requests, raw transport, failed native usage and exact command outcomes.

## Readout and decision limits

Rebuild provenance, sessions, containment, cost by profile, exact chapter counts,
and attribution at every checkpoint. Check that earlier chapter hashes persist
through later checkpoints and that all protected earlier experiments are unchanged.
Completion requires three accepted Wren chapters and one accepted opposing seed.

Record each Architect query's arguments, bytes, row count, selection total/hash
and pagination, including failed calls and any full-dump attempt. Bounded pages
do not establish economical retrieval; identify complete scans and repeated pages.
Record the actual grow request keys and all resulting accepted story positions.
Any newly accepted unplaceable record or executed unrestricted Architect world
dump defeats the corresponding engineering claim.

Read every new chapter in full. Trace acquisition, rank/point expenditure and
continuing conflict against the fixed source and chapter-to-chapter state. Inspect
the scale, ability prerequisites and completion errors for source distortion,
including whether ownership support survives legitimate numerical progression.
A model assertion of consistency is insufficient. Local diagnostic observations
can identify a repair target, but cannot label reader enjoyment or literary quality.

Report incomplete assignments, deviations and new failure causes alongside passes.
This sequential version comparison does not isolate a causal effect on token cost,
and one fixed story does not establish creativity or general serial reliability.
No reader score, production editorial intervention or quality qualification follows.

## Commands

```powershell
uv run python research/quality-measurement/world-fixed-continuation-20260913/run.py prepare
# Inspect, commit and push registration before generation.
runs/world-fixed-continuation-20260913/runtime/Scripts/python.exe research/quality-measurement/world-fixed-continuation-20260913/run.py run
runs/world-fixed-continuation-20260913/runtime/Scripts/python.exe research/quality-measurement/world-fixed-continuation-20260913/audit.py
```

Run repository handoff checks before the initial commit. After generation, validate
the derived evidence and its content-addressed claim record. Release only this
task's own lock when its sustained work has ended.
