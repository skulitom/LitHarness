# Results: the restored-directions chapter-one draw

**Draw 2 passed all four gates; chapter one was published to the operator's shelf and sent to
the operator.** The claim is **OBSERVED** (`claim.json`, after `audit`). `evidence.json` owns
the counts, flags, receipt hashes and each gate's item verdicts. The gate reads, the concept,
listing, world and chapter text stay under the ignored run folder
(`runs/restored-directions-draw-20260922/draw-N/`); none is copied here.

## Draws

| draw | writer | revision | calls | tokens | concept | listing | world | chapter | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | rowntree | `63dabc3` | 15 | 81,310 | PASS (C1-C4) | FAIL (L1 PARTIAL) | not reached | not reached | failed, shown |
| 2 | rowntree | `0ab40e3` | 33 | 423,223 | PASS (C1-C4) | PASS (L1-L4) | PASS (W1-W3) | PASS (H1-H8) | passed, shown |

48 calls and 504,533 reported tokens in all, on the Codex provider (`gpt-6-astra`, every role
strong), inside every per-draw ceiling. Every call passed every transport control the audit
checks (isolation, working directory outside the repository with `--skip-git-repo-check`,
model and effort, no memory, no project docs, no search, fresh sessions, the receipt chain), and
the first live invention request of each draw matched its offline preflight.

**Draw 1** passed its concept gate and failed listing item L1: the listing never named the
System or a rank. The cause located in the listing prompt was fixed in `406073f` (stage-0 §261),
and amendment 2 licensed draw 2 as a `fix`.

**Draw 2** delivered every restored text the registration checks in code (discovery v7 with
`WORLD_DIRECTION` and `READER_LIFE`, the development asks for the exception and `start_rank`,
`LIVED_WORLD` and the `stands_at` sentence in the seed, the first-use rule and its concept text
in the opening outline) and passed C1-C4, L1-L4, W1-W3 and H1-H8 on the coordinator's reads.
Chapter one ran four scenes, 3,909 words; the admin and frame lexicons counted 0 in the concept,
listing, world and chapter; no schema word reached the title, listing or chapter.

## What it does and does not establish

It establishes that, on one draw from one brief and one writer, the restored directions reach
the production requests and the gated artifacts show what the operator's enumerated items ask
for. It does not establish that a reader wants chapter two: the gate is the coordinator's read
on enumerated items, which has been miscalibrated on sentence feel before, and the operator's
read is the test. It is instruction delivery end to end, not evidence of reader response, and
the brief, a hook template, sidesteps open invention by design.

Residuals recorded at the gates, deciding nothing: water infrastructure and shelter as the arc's
spine again; a delivery-rider protagonist in both draws; the power hedged on the page; an
unranked start leaving the opening outline without a standing ladder; a restrained, domestic
texture that closes chapter one on a tarpaulin and a quarrel over the next kernel.
