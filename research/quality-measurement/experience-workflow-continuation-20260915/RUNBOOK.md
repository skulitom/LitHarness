# Explicit continuation: correct the experiment runner's scene identifier

Read the parent experiment RUNBOOK and the repository research governance first.
This continuation completes the already authorized second chapters. No story has been read
by the operator or reviewing agent between the initial run and this registration. Only
transport metadata, failure receipts and deterministic retention checks were inspected.

## Why the first run stopped

All eight books completed concept creation, world seeding/acceptance, outline, first draft,
evaluation and summary. The runner then supplied `architect grow --scene 1`. That command
expects a logical ID, unlike some diagnostic commands which also accept reading ordinals.
Each attempt raised KeyError before the completion boundary, with unchanged before/after
metadata, no queued or terminal job, and no provider call. The correct ID is `scene-1`.
This is an experiment-driver error, not an observed failure of either writing workflow.
The original runner, registration, receipts, stores and failed steps remain intact.

## Fixed continuation

Copy the eight closed canonical stores and their existing concept artifacts into a new
ignored run root. Verify byte-identical database copies and recorded head/scene hashes.
Keep both frozen production versions, source modules, dependencies, writer, initial premises,
plans, accepted chapters, summaries and normal settings. No content is edited or regenerated;
no production job is revived. Only the experiment's failed external command is corrected.

Resume the original four remaining phases: grow1 with `--scene scene-1`, accept-grow1,
ordinary ticks to chapter two, and drain its existing queue. Keep the original phase order,
counterbalance, per-phase cap, model controls and stops. Retain all new first outputs and
ordinary mechanical retries. No new premise, seed, concept, outline or first chapter is drawn.
No further continuation is authorized by this runbook after another operational failure.

Original usage was 96 completed provider calls and 2,100,789 Usage.total tokens, 12 calls per
book. This continuation permits only the remainder of the original aggregate ceilings:
144 further calls, 1,899,211 further recorded tokens, 33 further calls per book. The four-hour
clock still starts at the original dispatch, including this preparation gap. Admission may
cross a ceiling only for a call already admitted, as originally registered. No usage reset,
purchase, provider change or fallback. Hold the same owned machine lock throughout.

## Offline verification and audit correction

Before dispatch, invoke the corrected command against each copied store with the completion
boundary replaced by a recording refusal. All eight must resolve the scene, construct the
normal grow request, reach that boundary exactly once, and leave canonical metadata unchanged.
This preflight never invokes a provider. Test the ordinal failure and logical-ID success
through the actual CLI, plus negative transport controls, then run the repository handoff.
Commit and push this registration before any resumed generation.

Preserve the first auditor's false flags. The supplemental parent-controls record corrects
three implementation errors in that auditor, with no change to captured requests or outputs:

- An empty effective system gets the frozen adapter's exact default system sentence.
- JSON object serialization changed property iteration order. Strict-schema generation then
  produced a different order for the `required` array. Compare these arrays as sets with
  multiplicity retained by sorting; keep every other schema value and array unchanged.
- The private argument validator expects parsed allowance tuples. Parse the recorded
  allowances with the frozen adapter's own parser, then check exact command and interpreter.

All other transport, admission, fresh-session, tool-event and receipt checks stay in force.
Require the corrected parent controls to pass before continuation. Keep the original failed
audit alongside the supplement; do not silently rewrite it as a passing result.

## Reading and results

Continue to withhold all generated story content from review until this continuation stops
or completes. Then read every reached narrative draft and all discoveries, concepts and
outlines under the parent's descriptive protocol. Compare openings and continuations within
the same eight books. Report the initial driver error and preparation gap as deviations.
The result cannot be called an uninterrupted successful production run. Keep original and
continuation usage separate and report their sum without double-counting copied chapters.
No quality score, winner, popularity inference, candidate selection or production feedback
is licensed. Raw prose and reading notes remain in ignored run roots; commit only identifiers,
hashes, numerical evidence and methodological conclusions.

Commands: `uv run python research/quality-measurement/experience-workflow-continuation-20260915/run.py`
followed by `prepare`, then (after verification and commit) `run`, then `audit`.
