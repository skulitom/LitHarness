# Concrete world seeding

The operator found the repeated drowned-orchard settings inadequate and asked for improved
default seeding and experiments. This tests an authored input policy: material habitats,
embodied inhabitants, a local physical rule and a contested opportunity, with instructions
to make their properties consequential in early and later magical actions. These conditions
also apply inside the broad world's portals or eras. There is no water blacklist, output
filter, model ranking, novelty score or claim about the model's training distribution.

## Fixed comparison

Use three paired positions. Positions 0 and 1 deliberately repeat the seed labels from
automatic-seeding-20260910 cases 0 and 2, where drowned orchards appeared: respectively
83a9b641ef6d40ba95e9fb74ccd351a2 and 27bd0e944f6e4d52a51210ec58139587. Position 2 is one
fresh UUID drawn during preparation, retained without rerolling. All use deck index 0.
These are two targeted regression probes and one new draw, not a random population sample.

For each position prepare **baseline** from invention-seed.v1 with activity guidance, and
**world** from invention-seed.v2, which appends the concrete world input to that exact brief.
Keep the production discovery renderer, empty author brief, system, schema and parameters
equal within pairs. No prior output, diagnostic description or exclusion list enters either
prompt. Authored palettes contain aquatic habitats as well as other environments. The extra
world instructions and prompt length are confounded; this tests this complete intervention.

Order: baseline-0, world-0, world-1, baseline-1, baseline-2, world-2, development-1. The final
slot develops world-1 (the previously glacier-bound seed) with the production development
renderer, six scenes and third person. That position is fixed before outputs. Skip it if its
discovery is invalid, with no substitution. Validate with Concept.from_development; no
precision editing, listing, world creation, manuscript chapters or production acceptance.
Do not inspect story outputs until the whole batch finishes or an operational stop fires.

## Freeze and limits

Snapshot committed source at preparation's recorded revision, overlaying only invention.py
and cli.py. Exclude unrelated uncommitted precision work. Freeze all snapshot source, runner,
protocol, dependency lock, Codex binary, seed receipts and prepared discovery requests.
Preparation can refresh before dispatch, preserving all labels; progress.json forbids
refresh or implicit resume afterward. Commit implementation and registration before calls.

Own runs/box.lock after checking active processes. Run one sustained check or generation job
at a time. Use subscription Codex with requested gpt-6-astra, medium reasoning, no tools,
fresh isolated native sessions and no API keys, reset credits or fallback. Discovery requests
2,400 output tokens; development uses its production limit. Timeout: 600 seconds per call.
Stop after seven attempts or 80,000 recorded tokens, checked before each dispatch; the last
call can exceed that token bound. Stop on transport/auth/quota failure, unknown usage or
frozen-file drift. No retries or replacements. Never enable live-provider tests.

## Inspection and boundaries

After completion, generation_trace compares captured requests and native configuration,
checks session and output identities, and locates literal recurrences. Read every complete
discovery and development. Describe which habitat properties and inhabitant behaviors affect
the first reward and later use, whether the local rule is causal or merely decorative, and
whether recurring watery/infrastructure/permission structures remain. Inspect adversarially:
decoration around the old mechanism, contradictions between ingredients, loss in development
and active source adherence are all reportable. Search counts only locate passages.

No aggregate quality judgment or candidate selection follows. The default input policy is
the operator-authorized experiment, not a model-selected winner. Report bounded observations
and counterexamples equally, including any seeded water setting. Do not call input adherence
global novelty, reader appeal, unbiased sampling or a general success rate. Native receipts
do not expose every backend instruction or resolved sampling parameter. The one development
is pre-precision and cannot establish book-length preservation.

Raw prose stays under ignored runs/world-seeding-20260911. Commit registration, code, hashes
and bounded paraphrases. Audit can be rebuilt without a provider or book store:

```bash
uv run python research/quality-measurement/world-seeding-20260911/audit.py
```
