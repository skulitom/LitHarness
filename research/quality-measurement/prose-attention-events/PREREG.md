# Attention activated after source events

Registered 2026-09-07 before calls, following the operator's acceptance of the next phase.
The prior attention batch is closed: its single proposal was retained and no chapters ran.
This is a fresh representation test, not a repair, retry or completion of those missing draws.
No production writing policy or quality mechanism is being installed.

## Representation and exposure

Use the unchanged original 42-unit chapter map with the same eleven optionally implicit
context/knowledge units and 31 required-narration IDs. Preserve event order, knowledge timing,
scene division, quantities and endpoint. Both conditions must include the closure and purchases.
Original prose remains audit-only; normalized terminal punctuation permits the same existing
message words and occurrence order to fit their placement. No prior failed trace or draft enters.

Two source-derived calls produce one trajectory:

1. Initial state: receives ONLY the four reviewed opening-available units F01a, F01b, F03b and
   F06b. The latter two describe context and an instruction already known at opening despite
   their later positions in the source list. No later source, display wording, source notes,
   endpoint, previous reading or trajectory is supplied to this call.
2. Updates: receives the fixed initial state and full source. It supplies an after-state at each
   of twelve fixed activation units: F02a, F03a, F06c, F10, F13b, F17, F20, F23, F24, F26, F28,
   F29. Every state activates only AFTER its complete source unit. It cannot shape an earlier
   part of a compound unit. Source order, not prose paragraph number, determines activation.

Each state contains an immediate provisional concern, foreground and peripheral source IDs,
basis IDs and an unresolved question. Code supplies the before-state as the previous state and
connects it to the incoming unit and after-state. The model cannot rewrite a before-state to
contain its incoming event. Referenced evidence is restricted to opening-available IDs plus
the source prefix through activation. Empty periphery is allowed; no forced balance or number
of dramatic turns. State boundaries are fixture anchors, not a production narrative formula.

Structural tests reject unavailable references, changed activations, incomplete updates,
model-supplied before-states and state overlap. A complete semantic review must also accept
initial and updated text before any writer request is frozen. The updater still sees future
source; reference checks cannot prevent unlabeled future assumptions in natural language.
Major future knowledge, new canon or unsupported motive stops the batch. Retain all artifacts;
no repaired initial state, alternate update proposal, redraw or relaxation after inspection.
Compatible provisional concerns do not become mandatory canonical thoughts.

## Matched chapter comparison

Four independent complete chapters receive identical source, required-narration list, causal
trajectory, system and 1500-1800-word aim. Only rendering_mode changes:

- Background: states are available character understanding at their valid times; the writer
  makes ordinary choices of emphasis and transitions.
- Operative: currently available states guide emphasis and attention transitions. The incoming
  event meets the previous concern before the new state activates. Concern labels are not
  required exposition. Source actions and perceptions remain authoritative between anchors.

Order: background-1, operative-1, operative-2, background-2. Both draws are retained. There is
no fresh trace-free baseline. This estimates neither the value of adding character information
nor a general prose effect. Both conditions can use the shared trajectory spontaneously; an
operative writer can also merely explain it. One trajectory shared by four writers is one
derivation, not four independent replications of the mechanism. No writer sees sibling output.

## Readings and limits fixed before generation

Read the initial state, all twelve updates and all four complete chapters. Record each state's
source/knowledge compatibility at its activation, with explicit ambiguity or drift. Audit all
42 source-unit correspondences per chapter, separately marking optional omissions and required
losses. Record message occurrences and grammar, not just substring equality.

Locate realized attention before and after the twelve anchors. Does the incoming event meet
an existing concern? Does the next state change what receives detail, duration or a practical
response? Do peripheral facts receive less emphasis without disappearing from causal structure?
Does an unresolved question persist, or does narration immediately settle its significance?
Record absent and uncertain realization. Inspect opening, middle and ending, including rule
understanding, rescue, purchase and final status. More mentions of an object are not a quality
metric; explanatory sentences can provide necessary causality or interiority.

Distinguish enacted attention from restated concern summaries. The exact previous failure was
an invalid before/after representation, not observed prose failure. Passing this new temporal
gate only shows a usable proposal was delivered. Attention shifts likewise do not establish
better writing. Agent readings are defect harvest, not independent labels, rankings, selection,
effect estimates or qualification. Report OBSERVED only. No corpus or reader feedback is used.

## Dispatch, retention and stop

Six sequential Codex CLI calls maximum: initial-1, updates-1 and the four chapters. Requested
gpt-6-astra/high, existing signed-in ChatGPT subscription, isolated tool-disabled transport.
One full-1 transport slot per logical folder. Whole-batch stop: 125000 input + output +
separately reported reasoning tokens, including both prepasses. Cached input is already included.
An in-flight call may cross the stop; no next call then dispatches. No direct API, fallback,
probe, reset, purchase, retry or redraw. Test mode structurally disables all generation roles.

Inspect processes before/after taking runs/box.lock. Separate sustained checks and model calls.
Commit registration, generic runner and regression tests after handoff, before the initial call.
Freeze source/preparation/review, code/imports/CLI entry, systems and initial request first;
freeze the actual initial output before updates; freeze both actual outputs, temporal review
and exact writer requests before any prose call. Preserve every raw stream and failed request.
Keep source and generated text under ignored runs/. Commit only source-free code, registrations,
derived numbers/hashes and findings. Show all available artifacts in a browser-checked comparison.
Run final handoff before commit/push, verify remote main and archive only the owned lock.
