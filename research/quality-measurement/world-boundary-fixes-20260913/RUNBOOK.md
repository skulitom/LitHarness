# Supplied-story boundaries in development and world seeding

The operator asked to fix world generation and the other located problems on 2026-09-13.
This run tests the changed input contracts and scoped retrieval on new stores. The prior
three-chapter Wren book stays frozen. No reader output, diagnosis or prior chapter enters
generation. This is a source-preservation diagnostic, not a quality gate or a claim that
repetition is solved.

## Fixed assignments and changes

1. The same first Opus Wren expansion used by the completed continuation case, identified
   by the receipt hash in `run.py`. One development, one world seed, one accepted chapter
   and one reconciliation. Retain the first result of each role without replacement.
2. A constructed opposing treatment in the ignored run root, hashed in registration.
   Its protagonist is explicitly Mara, wants to return home, already owns two named
   capabilities, must meet a fixed two-trial quota, and faces a permanent exclusive fork.
   One development and one world seed; no chapter. This tests against suppressing these
   features indiscriminately. It is a synthetic boundary case, not an independent book
   sample or evidence of audience preference.

Seed v5 removes the unconditional permanent-fork instruction, makes supplied access
conditions authoritative and leaves unspecified quotas/additional cast open. The seed
gets the exact opening and growth in a separate pending-intentions block, because
properties alone did not identify unfinished acquisitions in the previous case. World
properties remain separately projected; future events must not become declarations.
This reintroduces source context at seeding and therefore needs observation for leakage.
Reconciliation gets the actual chapter, without replaying that pending block. Development
v6 asks for the source's motive rather than adding a new one. No name or setting is banned.

CLI/MCP `world show` now share batch subject and exact predicate filters and can omit
superseded declarations. Existing unfiltered history remains available with provenance;
`current` includes proposals and story-time changes, not a historical snapshot. Seed/grow
instructions advertise scoped reads. Record actual reads and bytes without attributing
aggregate native token costs to individual tool responses.

## Dispatch and stops

`prepare` archives committed source/migrations, creates a runtime importing that snapshot,
and hashes the native executable, dependency inventory, runner, runbook and both sources.
Commit and push registration before the first model call. The native executable must be
`codex-cli 0.154.0-alpha.6.2`, authenticated with ChatGPT. Use the repository default Codex
model/reasoning, with no API-key transport, provider fallback or relaxed containment.

The driver is a scoped copy of the prior continuation driver. Each fixed Discovery enters
the ordinary development request and `Concept.from_development` once. No invention,
precision pass, name redraw or source correction. Use ordinary `new --concept`, with a
fixed neutral premise/title, six scenes, one scene per chapter, six chapters per arc and
1,800 target words. This omits listing to isolate the boundaries; it is not a full listing
comparison with the prior run. Seed/check/accept and tick/grow/check/accept use normal CLI
policy paths. Save empty, seeded and final checkpoints, including full world history.

Keep one sustained job under `runs/box.lock`. Bounds: 40 native attempts / 1,600,000
reported tokens total, 25 attempts / 1,000,000 tokens per book, and a two-hour new-call
wall bound. Existing per-call timeouts remain. Ceilings are checked before calls; an
in-flight response can exceed them. Account for health and rejected native usage, and
stop on unknown usage, transport/containment failure, input/runtime drift, attribution
failure, unexpected chapter count, or twelve ticks without progress. Stop the whole run
on either book's execution failure. Preserve attempts and do not implicitly resume.

## Readout fixed before dispatch

For every stage, retain exact requests/results, bridge trace, hashes, sessions and native
usage. Verify original source and opening preservation, empty initial stores, accepted
revision attribution and unchanged pre-existing Wren artifacts.

Read development fields, seeded declarations, Wren's outline and the complete new chapter.
Report concrete source-to-stage locators for:

- Acquisition timing: Wren's assessment must still be pending at seed; the opposing
  case must retain Fold and Anchor as already owned and leave Step pending.
- Access: Wren's Split/Articulated route must remain jointly attainable; the opposing
  case must retain its explicit permanent Mirror/Echo exclusion.
- Motive and extra details: trace any added return-home pursuit, familiar cast name or
  new advancement quota in Wren; verify the explicitly required versions in the control.
- Source leakage: future meetings, completed tests, gains and discoveries asserted at
  the opening, and known conditions elided by the Wren outline/chapter.
- Retrieval: scoped versus unfiltered reads, repeated identical payloads and returned
  bytes. Native usage is separate, with no causal savings estimate across different runs.

These are located diagnostic observations, not scored reader answers. Failure of any
source boundary prevents a claim that the relevant defect is resolved. Even preservation
of all these items would not establish creativity, reader enjoyment or general reliability.
No automatic candidate ranking, reroll, or editorial feedback follows the readout.

## Reproduction

```powershell
uv run python research/quality-measurement/world-boundary-fixes-20260913/run.py prepare
# Inspect, commit and push registration before generation.
runs/world-boundary-fixes-20260913/runtime/Scripts/python.exe research/quality-measurement/world-boundary-fixes-20260913/run.py run
```

Run `tools/check.py handoff` before committing engineering and before final handoff.
Inspect saved CLI views first; preserve the other session's work and release only this
task's lock. Report every first output, including stopped or adverse results.
