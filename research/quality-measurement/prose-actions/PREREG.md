# Action planning diagnostic, 2026-09-06

CONJECTURE: constructing immediate attempts, observable responses and consequent actions
before prose may change the explanatory and introductory habits located in the previous
opening diagnostics. This is a five-call feasibility trial on one source, not qualification
of a quality measure or an autonomous planner. No ranking, selection or production adoption.

The operator authorized trying this method and earlier authorized committing and pushing.
Use ChatGPT subscription authentication through the installed Codex CLI only. No direct API,
API authentication, reset, fallback, redraw or retry after a dispatched failure. The previous
Codex diagnostic's exact transport and startup-notice parser are reused. Requested model
is gpt-6-astra at high effort throughout; independently resolved model may be unavailable.

## Frozen source and intervention

Use the full neutral source request from the earlier framing diagnostic. Before any model
call, create an explicit source amendment in the ignored run directory and freeze it with
the original and reconciled requests. Resolve movement eligibility as stepping from the
current position, retaining the standing start; clarify the recorded initial ability/fitting
as an entry grant exempt from the listed later acquisition cost. These are authoring choices
for this test, not discoveries about the original canon or automatic database repairs.
No original source file, manuscript, canon or production prompt is modified.
The amendment also makes the stop at the light change and the first attempted step at the
endpoint explicit, so payment is not silently deferred. It distinguishes the unspecified
paper countdown from the personal no-attempt count without assigning the former a new meaning.

Both drafting conditions receive exactly the same reconciled background, system instructions,
rules, viewpoint, word target (approximately 900), starting situation and endpoint. Control
keeps the original ordered action list. Treatment replaces that list with a source-reviewed
plan produced by one planner invocation. The planner receives only reconciled source and
stable required-action IDs, never previous prose, critique, reader output or corpus material.
It may connect required events with present-scene attempts and responses; it may not change
required events, knowledge limits, dependencies or the ending. Overlapping events may overlap.
All facts outside the replaced action section remain byte-identical between conditions.

Read the entire proposal against source before drafting. Record every source correction in
a retained note; no stylistic polishing or candidate selection. Freeze the reviewed payload
and both rendered requests. Any unresolved source conflict or proposal that cannot be made
source-consistent without redesign stops the draft phase. Do not merely erase conflicts.
ID coverage is only a structural check and never proves semantic fidelity or scene quality.

## Calls and stops

One Codex planner, then four fresh Codex drafts in order control-1, actions-1, actions-2,
control-2. For transport reuse their cache IDs are planner/full-1 and drafts/full-1,
focused-1, focused-2, full-2 respectively; those names do not denote context trimming.
Maximum five invocations. Before each new call, stop if cumulative input + output + separately
reported reasoning tokens reaches 120000. Cached input is already within input. This is a
dispatch stop, not an exact cap on a final call's output. Missing/failed usage, actual tools,
unrecognized runtime errors or changed frozen artifacts stop the run. No external dollar
charge is inferred from subscription usage; unavailable cost is reported as unavailable.
No concurrent model arm or sustained checks; take the shared box lock after inspecting processes.

## Reading and interpretation fixed before calls

Read all four complete scenes in generation order. Retain and locate: actions and responses
that carry source events; background introduced without affecting an immediate choice;
repeated explanations of the same rule; new or inconsistent physical/timing/system facts;
and departures from the required endpoint. Do not tally these as a quality score or infer
AI authorship from a sentence shape. These observations are agent defect harvest, not labels.
Keep all outputs, including inconvenient results. The full prose comparison uses fixed
condition positions and no preferred default selected after reading.

The source amendment is shared by both conditions, but the treatment still bundles changed
plan content, length and representation plus an extra planning call. A difference cannot
identify which component caused it. Two draws per condition on one story cannot establish
reliability, transfer or literary improvement. This trial does not license reader steering,
candidate selection, narrative-specific production rules or a production quality gate.
