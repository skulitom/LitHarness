# Execution qualifications discovered during retained-artifact review

2026-09-07, recorded after dispatch. This does not amend the frozen comparison or replace
any output. The registration's internal no-retry condition was not fully met.

## CLI sampling retry

The separated-free-1 raw stderr reports a dropped WebSocket stream followed by an internal
sampling retry. The wrapper made one logical CLI invocation, whose stdout contains one
completed turn and one final chapter. This is not proof of one sampling attempt. The CLI
warning was found during raw-artifact review after the last registered invocation had already
started. That last call completed; no new call, replacement draw or repair followed discovery.

Retain separated-free-1, show the warning beside it, and exclude it from any claim requiring
an uninterrupted fresh sampling attempt. Do not describe the batch as eight retry-free
samples. Its earlier partial sampling state and complete account-level consumption cannot
be reconstructed from the final usage record. The remaining retained outputs can supply
located counterexamples to a general repair claim, not a clean estimated factorial effect.

All stderr is retained. separated-free-2 also reports an analytics-send failure after its
generation; separated-target-2 reports an available-model refresh timeout before its turn.
Neither warning records a sampling retry or proves a fallback model. Requested model and
effort remain verified from argv; resolved model and the complete upstream context are not
captured. All calls report the usual unsupported PowerShell snapshot warning.

## Reporting the frozen token counter

The registered runner sums input, output and separately reported reasoning fields for its
stop. Preserve that exact counter and ceiling; do not change dispatch accounting after calls.
Report the original fields and input-plus-output separately, without interpreting either
as subscription-window usage or silently treating a reasoning field as disjoint consumption.
The accompanying result records the authoritative accounting audit and any correction to
earlier terminology. No reset, purchase, direct API or alternate-provider call occurred.
