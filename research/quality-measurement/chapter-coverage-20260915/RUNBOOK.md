# Fixed-story chapter-coverage comparison

Read BRIEF.md, EPISTEMIC_GOVERNANCE.md and the parent workflow RUNBOOK before dispatch.
The operator authorized the implementation and a smaller comparison holding upstream story
material fixed. This is an isolated authoring experiment, not a reader mechanism.

## Question and fixed units

Does the new chapter grouping preserve or explicitly adapt the intended opening coverage
when the same concept and world reach the real outline-to-draft workflow? The implementation
is aa33e6a; control is d247331. Control already includes the automatic experience brief.
The changed discovery/concept input layout is covered by offline tests; this comparison
isolates the planning change and does not redraw invention to test that upstream effect.

Use the prior trial's B2 and B4 concepts and pre-outline worlds, selected because their
opening-chapter coverage conflicts were explicit. These cases and their prior results are
already known; this is a diagnostic comparison on known cases, not an unseen transfer test.
There are two independent story units, each with control A and treatment B. Generate fresh
outlines and two chapters per arm: four books, eight chapters. Read every reached outline
and draft, keeping failures and ordinary mechanical retries. No candidate ranking or quality
selection. Reused concepts, seed declarations and author briefs remain byte-identical.

## Reconstruct the common starting state without generation

Archive both committed source versions and create isolated runtimes with the same dependencies.
Use the original short premise, saved concept and original book ID in each arm's separate
database. Run ordinary `new`, then replay only the successfully executed `world declare-batch`
arguments from the original seed call, in their recorded order, and `world accept`. Do not
replay malformed requests, diagnostics or any unrecorded command. Validate replay arguments
through the frozen provider's original world-tool allowance parser before executing them.
No model is called during preparation; test-mode transport and a refusing boundary enforce this.

Retain the original command receipts, reconstructed stores and all preparation outputs.
Frozen starting-store copies remain separate from live experiment stores. On separate copies,
run the actual production selector/outline handler with a refusing recording provider.
The control request must equal the original trial's first-outline request exactly. Treatment
must differ only in the registered chapter-layout field and rule, output schema and profile.
The concept, world, author brief, state, scene map, length target and remaining request fields
must match. Refuse dispatch if these controls fail. Preparation may be corrected before
registration/dispatch; keep failed preparation receipts. Never overwrite a dispatched run.

## Production run and resource boundary

The only live phases are chapter1, drain1, grow1 using logical ID `scene-1`, accept-grow1,
chapter2 and drain2. Use the ordinary CLI and unchanged application policy for every phase.
World growth after chapter one follows each arm's own accepted story; only pre-outline story
material is held fixed. One scene per chapter, six chapters per arc, target 1,400 words per
scene, writer halloran, third person. Alternate arm order within and across the two cases.

Use the existing signed-in native Codex subscription, gpt-6-astra / medium, one call at a time,
ephemeral sessions, no user/project instructions or memory. Only the production world-command
bridge is allowed; no web search or other tools. No model fallback, usage reset or purchase.
The recording wrapper changes no model input, timeout, schema, allowance or application result.
Use the parent recorder and the continuation's corrected transport auditor, frozen by hash.

Ceilings: 80 provider invocations including health probes, 2,000,000 recorded Usage.total,
20 calls per book, two hours from dispatch, 20 ticks per phase. An admitted call may cross a
ceiling; report actual usage. Stop a book on a terminal job, operational exit, non-tick command
failure, no_work before the target or tick cap. A provider/transport failure or unknown usage
stops the whole run. Keep original first outputs. No manual world repair, job revival, story
redraw, changed prompt or implicit continuation. Hold the owned machine lock; no sustained
checks alongside live generation. No LITHARNESS_LIVE_PROVIDERS during offline checks.

## Verification, dispatch and reading

Test the replay whitelist, request-difference control, exact chapter membership refusal,
continuation coordinates and production persistence. Run the repository handoff. Prepare and
commit/push the runner, tests, RUNBOOK, registration and claim before any model call.
Then run `run`, followed by `audit`. The runner refuses test-mode, duplicate and uncommitted
dispatch. Its native executable, archived source, dependencies, initial states, common inputs
and offline requests are content-addressed. Transport and canonical attribution must pass.

Do not read any new story output until all generation completes or stops. Then locate the
same proposed activity, use, consequence, response and subsequent choice in each outline and
both drafts. Distinguish planned future events from enacted prose and an explicit scope
adaptation from unchanged coverage. Inspect frozen writer inputs for apparent handoff gaps.
Include contrary cases, strengths in either arm, costs that matter, and failures to deliver
the proposed experience. Do not treat correct grouping as semantic fidelity or enjoyment.

Report all four books, resource counts, mechanical retries, operational failures and every
control. This trial has no scoring, preference, popularity or quality-effect decision rule.
The claim may become observed, never supported literary quality or qualified production
reader authority. Generated prose, input text, narrative notes and report remain under ignored
runs. Commit identifiers, hashes, numbers and methodological conclusions. No reading note or
dossier enters production generation or editorial control.
