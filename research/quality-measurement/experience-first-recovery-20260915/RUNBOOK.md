# Continuation after the operator's computer shut down

The operator explicitly asked to continue after a shutdown. This registration amends only
the interruption handling of [the original pilot](../experience-first-20260914/RUNBOOK.md).
It does not revise its creative prompts, original briefs, seeds, source snapshot, analysis,
or downstream handoffs. The original registration and all original receipts remain unchanged.

## Preserved work and the one repeated request

The original run completed 56 requests: 12 preliminary proposals, 18 discoveries, 18 concepts,
and 8 outlines. It saved a started receipt for outline-C-3, then the machine shut down. No
research process remained after restart. The recorded native final-output path did not exist;
the trace contained request metadata and no result, events, return code, or usage. This proves
absence of a locally saved final response, not absence of server-side work or resource use.

Before any story content is read, copy the 56 completed receipts, existing requests, and seeds
byte-for-byte to a separate continuation root. Preserve and hash the original interrupted
receipt and native trace. Execute the same saved outline-C-3 request once in a fresh session,
then the untouched remaining order. There are at most 28 new calls. A completed response may
never be regenerated or substituted. A further interruption or transport failure stops this
continuation; it does not authorize another automatic retry.

The complete assembled dataset may therefore contain 84 completed outputs from 85 attempted
calls, with one interrupted attempt of unknown outcome and usage. It must not be described as
an entirely unretried first-attempt sample. All 56 pre-shutdown responses are inherited first
outputs; the remaining 28 slots receive their first locally complete response in this phase.
Record the shutdown boundary as a potential temporal confound. Do not claim a model effect
from a difference between phases.

## Source, controls, and limits

Use the original archived source and exact native executable after verifying their hashes.
The recovery driver loads the unchanged original runner, selects its archived imports, then
points its request builder and auditor at the continuation root. The auditor's second source
selection is disabled because imports have already been selected. The complete slot order,
renderers, schemas, and parent-validation behavior remain the original code.

The manifest freezes the recovery driver, this runbook, its tests, the original frozen files,
the original checkpoint, every original request/receipt/transport trace, and all inherited
copies. Verify these before each new call. The continuation registration must be committed
and pushed before dispatch. No corpus text or generated content has been inspected to choose
the recovery path, and no corpus material is a generation input.

Keep the original 180-minute admission deadline measured from 2026-09-14T22:58:10.151755Z.
Stop new calls when known recorded tokens, including the original 406,523, reach 2,500,000.
The interrupted attempt's unknown usage remains additional and unestimated; this is a bound
on known reported usage, not a complete spend cap. Preserve the original timeouts and requested
gpt-6-astra / medium configuration. Run one call at a time under the original owned box lock.

Run offline tests and the repository handoff check with one worker before committing the
registration. From the repository root:

```powershell
uv run python research/quality-measurement/experience-first-recovery-20260915/run.py prepare
uv run python research/quality-measurement/epistemic_governance.py research/quality-measurement/experience-first-recovery-20260915/claim.json
```

After commit and push:

```powershell
uv run python research/quality-measurement/experience-first-recovery-20260915/run.py run
uv run python research/quality-measurement/experience-first-recovery-20260915/run.py audit
```

Do not inspect story content until this continuation completes or stops. Read every reached
output and follow the original descriptive analysis. The same boundaries apply: no scores,
rankings, winner selection, simulated reader feedback, or qualified enjoyment/popularity claim.
Reading notes and story prose stay under ignored runs; tracked evidence carries identifiers,
hashes, counts, and methodological conclusions. Release only the owned lock after sustained
work finishes. The old checkpoint remains preserved as the state at shutdown, not rewritten
to imply an orderly stop.
