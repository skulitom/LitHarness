# Refresh admission with the available newer production books

This is the operator-authorized call-free refresh, using the September 13 continuation's
frozen interpreter. Read `BRIEF.md`, `EPISTEMIC_GOVERNANCE.md`, and
`plan/causal-salience-interventions.md` before interpreting it. No reader validity claim
or paid reader run is registered here.

Retain the September 10 census's entire source list, including missing stores, and add
`runs/fresh-chapter-review-20260912/book.db` and
`runs/distinctive-continuation-20260913/book-1/serial.db`. A filesystem inventory on
September 13 found these as the newer canonical production stores in dated run roots;
exclude preflight stores and before-tick snapshots, which are not independent books.
Recent invention/planning/chapter-only experiments have no accepted store relations and
are outside this admission surface. This is a refresh of that cohort, not all local books.

Wait until the continuation run is finished, even if stopped. Hold only this task's
shared-machine lock and run no generation or sustained validation concurrently. Set
`LITHARNESS_ENV=test` to structurally refuse paid providers. Copy each existing source
through SQLite's read-only backup connection, migrate only the disposable copy, and
enumerate all branches. The original database hash must remain unchanged.

Use the prior census's `reader-evidence-audit` and `corpus_io.generated_scenes` workflow.
Pass the registered chapter shape for each newer source: one scene for the two
continuation attempts, four for the September 12 book. Keep default shape for the older
fitness and volume sources. Save full prose and hidden keys only under ignored `runs/`.
Report hashes, every candidate family and rejection reason, exact-span admission,
ecological item counts, and independent book IDs separately from branches.

```powershell
runs/distinctive-continuation-20260913/runtime/Scripts/python.exe research/quality-measurement/causal-reader-admission-20260913/census.py
```

Zero state-continuity items or inadequate independent books stops a reader arm before
spend. Other family candidates still require implemented, independently verified damage
and matched harmless transformations. Do not weaken admission, infer hidden keys from
agent reading, add evidence to canon, or promote prose diagnosis into generation.
If usable material exists, a separate registration must freeze texts, book and
transformation holdouts, fingerprint controls, attainable ranges and spend before calls.
