# Refreshed admission still has no ecological test items

**State: OBSERVED.** The frozen September 13 pipeline audited read-only backup copies
of the entire earlier cohort plus the two newer canonical production stores. There were
26 available stores from 27 designated paths and 27 distinct book IDs across 27 branches.
The earlier baseline's third store remains missing. Every existing original database hash
was unchanged. No model was called; `LITHARNESS_ENV=test` structurally refused paid providers.

| Candidate family | Admitted relations |
| --- | ---: |
| State continuity | 0 |
| Event consequence | 0 |
| Progression cost | 0 |
| Character cause | 0 |
| Promise/payoff | 54 |

The September 12 production book contributes one promise/payoff relation; the new Wren
book contributes two. Neither adds any state-continuity item. The historical cohort's
51 promise/payoff relations remain, making 54 across the refreshed cohort.

The ecological generator still emits **zero items**. All branches remain ineligible for
a model run under this battery. Promise/payoff candidates do not supply a verified damage
and matched harmless transformation: that implementation and its controls remain absent.
This is an admission limitation, not evidence that a reader failed or that the prose is good.

Wren's audit rejects additional promise candidates for missing unique evidence and causal,
event and progression candidates for missing roles. Its three exported scenes contain
6,169 words in the corpus loader's text representation. The September 12 book contributes
four scenes and 3,810 words. Chapter-file rendering can include additional status labels;
these export lengths are not quality scores or independent judgments.

`results.json` contains source and backup hashes, source-code identities, all branch
censuses, rejection counts, manuscript digests and public/private packet hashes. Full text
and hidden keys remain under ignored `runs/causal-reader-admission-20260913/`.
The fixed source-selection and backup workflow are in `RUNBOOK.md` and `census.py`.
The executed census driver is preserved in commit `ad39dc0` and identified by the
`driver_sha256` in the results; a later line wrap fixes lint without changing its behavior.

The next reader experiment still needs an implemented, independently verified transformation
for an admitted relation, matched harmless edits, whole-book and transformation holdouts,
and an attainable registered decision rule before spending on reader calls. This refresh
does not weaken those conditions or feed its diagnostics into generation.
