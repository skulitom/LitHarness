# Opening allocation and retained consequence

The operator asked to continue work on the six chapters' weak points after the
scene-focus trial. This is one isolated author-directed structural revision.
The project-authored `pre_registration` block in `directions.json` fixes two
concrete story decisions: reach drinking water within
Chapter 1, and carry a materially failed bedding attempt through its chapter before
a later qualifying test and Duration purchase. These directions are formulated by
Codex under that delegated request, not literal user quotations or qualified reader
feedback. They deliberately prescribe events and timing for this book. They do not
become general production defaults, a craft metric or an automatic feedback loop.

The historical contrast is the first retained
[scene-focus output](../scene-focus-20260913/REPORT.md). Its opening allocation and
softened consequence motivate this attempt, but the report, diagnostic text, old
chapters and reading notes do not enter generation. The new standalone directions
contain story decisions, not evaluations of prior output. No model ranks, scores,
selects or repairs a candidate.

## Exact intervention and controls

Archive production source at `14fd7fb`, without working-tree overlays. Reconstruct
the same first retained growth seed, preserving record identities and creation
times, and copy the same original concept unchanged. The layout remains six scenes,
one per chapter, with a requested 1,800 words per scene. This tests allocation of
events within that layout; it does not compare scene counts, chapter sizes or the
production default layout. Keep model/reasoning defaults, verbatim bridge replies,
mechanics and ordinary manuscript acceptance/reconciliation unchanged.

Before freezing the fixture, submit each direction with explicit delegated-research
provenance through the existing verbatim directive lane. Run its deterministic
conductor job with the fake provider under `LITHARNESS_ENV=test`. Require an applied
directive and recorded `directive.verbatim.v0` policy decision. Confirm seed world
equality after installation. Record each directive, plan-item and decision identity
and body hash. The production director-agent prohibition remains intact; this
research fixture does not represent a model Director's independent authority.

Hash source, runner, audit, helper chain, directions, tests, dependencies, native
binary, concept, prepared database and protected prior artifacts. Commit and push
registration and exact scripts before live calls. Registered predecessor helpers
remain frozen and are loaded privately with this run's paths and limits. No live
input changes or source patches are allowed once calls begin.

## Execution bounds

Check processes and acquire `runs/box.lock` with owner prefix
`opening-consequence-20260914:` before sustained checks or generation. Release only
this task's completed lock. One sustained job runs at a time.

Ceilings: **32 native attempts, 2,200,000 reported tokens, 90 minutes for starting
calls, six chapters**. Health and all sub-calls count; an in-flight call can overshoot.
Retain the first output and every failure. No native retry, reroll, fallback,
implicit resume, budget increase, manuscript patch or chapter extension.

Stop on lost lock, source/input/runtime/binary drift, unknown or mismatched usage,
transport or containment failure, failed world checks or revision attribution,
altered earlier text, excess chapters, or twelve ticks without expected progress.
Record immutable database checkpoints and chapter hashes after reconciliation.

## Readout fixed before calls

Reuse the provenance audit and supersession-aware seed reader. Retain historical
false flags, diagnostic exits, partial sheets and unresolved comparisons. Check
exact direction text in actual outline locks and all writer systems; verify the
unchanged six-chapter mapping, word target, scene instructions and opening scope.
Verify direction preservation and unchanged checkpoint databases during reading.
These are routing and integrity checks, not literary efficacy measures.

Read every produced chapter in full, locating contrary as well as supporting cases:

1. Does Chapter 1 reach water while retaining the material mistake, changed method,
   actual magical limits, causal bearing/slip distinction and companion encounter?
   Record rushed or unclear transitions introduced by compression. Completion alone
   does not establish a more satisfying opening.
2. What occupies the freed chapter space? Does it develop earlier choices and
   relationships, repeat another procedure, or invent a convenient new task?
3. Is the bedding actually unusable, the repair unfinished and the route unavailable
   at the failure chapter boundary? What action occurs under that unresolved cost
   before the later reward? A chapter break alone is not a sustained consequence.
4. Does the later test fit the starter load limit and meet novelty/measurement rules?
   Is the lost material still lost after Duration, with fresh resources and renewed
   cooperation required? Preserve contrary compensations and unearned bargains.
5. Do the memorial relationship, other participants' interests, retained expertise
   and Load, ordinary permanent-repair mechanism and unresolved marker survive?
   Track actual casts and numerical coverage separately from editorial interpretation.
6. Locate procedural repetition and explanatory restatements that persist, as well
   as uncertainty that requires explanation. Do not score word counts or sentence
   forms, or claim broader emotional development from this structural intervention.

One changed-input historical comparison cannot separate ordinary draw variation,
the two directions or their interaction. A completed run with remaining defects is
a result, not grounds for extension. Raw prose, receipts and databases remain under
ignored `runs/opening-consequence-20260914/`. Commit derived data, hashes and a
bounded editorial report; no quality qualification follows.

```powershell
uv run python research/quality-measurement/opening-consequence-20260914/run.py prepare
uv run python tools/check.py handoff
# Commit and push before generation.
runs/opening-consequence-20260914/runtime/Scripts/python.exe research/quality-measurement/opening-consequence-20260914/run.py live
runs/opening-consequence-20260914/runtime/Scripts/python.exe research/quality-measurement/opening-consequence-20260914/audit.py
```

Run handoff before the result commit. Do not edit the registered scripts after calls.
