# The twentieth operator read — *A Thousand Skills from Home*, chapter one

**Status: DEFECT HARVEST, 2026-09-23. Not data (§95).** The operator read draw 2 of the
registered restored-directions draw (`research/quality-measurement/restored-directions-draw-20260922/`):
the listing and chapter one of *A Thousand Skills from Home* (rowntree, Codex `gpt-6-astra`),
which had passed every coordinator gate (C1-C4, L1-L4, W1-W3, H1-H8). His words verbatim, the
analysis ours.

## 1. The items

> *The premisis reads like a list of facts instead of an overview which should grab attention
> with a hook*

Of the listing ("Theo Marsh knows his delivery routes by heart. Then the System arrives… Each
new ability starts at Skill Rank 0. Raising his Personal Rank takes harder trials…").

> *"He had reported it with his rent payment last month" - sounds like a leak of debts and
> promises, the same issue as from the very beginning, its like debts, ledgers etc has to be
> mentioned for some reason, and i don't understand why we can't get rid of it.*

Of chapter one's first paragraph.

> *"Between her lowest surviving tread" what is a surviving tread?*

> *I would probably stop reading around overview/ beginning*

## 2. What it says about the gate

The coordinator's gate passed all three. L1 was read as satisfied because the listing named the
System and its ranks, which §261 had just asked for; the operator reads that as a list, not a
hook. H2 was passed with the rent line noted as "the nearest is the opening joke"; the operator
reads it as the recurring money register. Odd diction is not a registered item at all. The gate
was right on structure and wrong on all three things that decide whether he reads on.

## 3. Routed, and fixed at §262

Each item went to a located cause before any fix. §262 of `plan/stage-0-decisions.md` records
the causes, what changed, what was held, what went to the operator and what none of it
establishes.

- **The list of facts (items 1 and 4).** §261's clause listed what the listing had to name (the
  game system, its skills, its ranks, and a counted rank as never incidental), and the listing
  named each in a run of short stand-alone sentences. `writer.overview.concept.v4` names the game
  system once and asks for a hook, and §261 is corrected in place.
- **The rent (item 2).** §255's `discovery.READER_LIFE` said "a job that covers rent", outside the
  guard §174 wrote against that phrase. It reached both concepts drawn, then the world records,
  every scene packet and the page. It now says "a job" and the guard covers it. The concept's
  debt-named keys are shown to a model under neutral names, and the Architect's payment wording,
  the source of "not currency" in every scene, is reworded.
- **"Surviving tread" (item 3).** "Surviving" reached the writer only through the planner's scene
  brief, which restated a capability limit; "tread" came from no input. The planner no longer
  lists capability limits among a scene's future dependencies. A scope sentence for world rules is
  held until a census of stored rules justifies it.
- **The gate.** The next chapter one comes from `tools/chapter_one.py`, gated on items tightened
  to the three things he named (C-money, L1, L3, L5, L6, W-money, H2, H-open, H-diction), with an
  inert register report beside each gate. Nothing here was drawn, so that chapter's read is the
  test.
