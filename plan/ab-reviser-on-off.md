# A/B redraw: reviser-on-off

**Status: read.** Two draws under one settled listing. This
document is written BEFORE either arm runs and is the thing that makes the arms admissible.

## The standing caution (stage-0 §105, verbatim, and it does not move)

> The agentic path bought nothing on these cases and cost 2.25x the calls.

> No general prose hill-climbing, and no selection among mechanically valid candidates by any
> quality proxy, score, ranking or preference signal — enforced by an import ban, not by
> intent.

**So: a variant comes from a diagnosed defect, never from undirected variation.** Name the
defect and where it was diagnosed, or do not run the arms.

- **The diagnosed defect this answers:** the reviser rewrites every shipped sentence at
  109% of the writer's cost with three measured defects (plan/agent-impact/reviser-impact.md:
  subordination flat, gloss introduced 1→3 — read 13's first item — and its §54-style
  control arm never drawn), and reads 13–14 both landed items on reviser-processed prose.
  Whether the reviser's rewrite is net-positive ON THE SAME LISTING has never been measured
  within one draw family; every prior comparison crossed draws with the writer moving too.

## The variant, by §-entry

- **Arm A** — the pipeline as merged at 22ff66d, reviser ON (§185–§188 as they stand).
- **Arm B** — identical pipeline with `--no-revise` (§185's hash-identical control arm,
  drawn for the first time), and nothing else differs.

The arms differ by exactly the entries named above. Anything else that moved between them is
a confound and belongs in the control statement below, named rather than assumed absent.

## The control statement (§54's shape)

§54 stated its arms as *"Same premise, same model, same budget, 30 scenes"* and then wrote
down what it could not hold constant — *"And the confound, which is not small."* Both halves
are required here.

- **Held constant:** the listing (title and premise, byte for byte — the harness records a
  sha256 of each and refuses a second arm under a different one), the writer, the scene
  count, the chapter size, both spend ceilings, a fresh store per arm.
- **Not held constant, and named:** the seed draw (each arm draws its own world from the
  same listing — worlds differ, which is the standing two-draws boundary), sampling
  temperature throughout, and the date (2026-09-01, one sitting). Model versions, roster
  state, writer (sandoval), scene count and tick recipe are constant. Any
  other track merged between the two arms — anything that moved besides the variant.
- **n is one per arm.** Two descriptions, never a treatment effect; the conclusion section
  must say so again, and does.

## What this experiment may and may not produce

A description of two books, side by side. It may not produce a win, a rank, a score, a bar,
or a treatment effect: two draws are two draws (`plan/serial-pilot-15b.md` §0), and no bar is
declared anywhere without §61's four attainability checks. A person reads the two folders.

## The reading

Both arms landed 2026-09-01: arm A (reviser on) $5.16/5 calls, shelf `--3df937dc`; arm B
(`--no-revise`) $3.57/3 calls, shelf `--63840e5f`. n is one per arm; every line below is a
description of two draws, never a treatment effect.

**What the four signals showed.** Scorecards: B sits at the market's mundane-number rate
(0.99/1k vs A's 3.68 against the genre's ~1.1), moves a number on its printed sheet
(Graded 11→12; A's sheet never moves), and carries two 6+-join chains to A's seven and one
prose em dash to A's four; A wins the 4+-chain share narrowly (0.127 vs 0.143). The
coordinator's hand read (demoted protocol, descriptive): B reads better — voice, dread
mechanics (the counter dropping while the yard writes), an economics antagonist, market-rate
numbers. The exploratory panel (its own provenance sentence applies): 13–7 toward A, but
first-slot share 0.65 equals the preference share and seven of ten personas split 1–1 by
order — the order-robust minority (grinder, skimmer, stylist) took A in both orders. The
adversarial battery, run in both directions on the sentence axis: damage_survival and
sham_separation FIRED both ways — neither arm's sentence-axis margin exceeds its own
shuffle-invariance or the same-draw window noise. No surviving provisional win; the critics
were therefore not spent.

**What they cannot show:** anything about the reviser as a treatment — the worlds differ
(the fork is arm A's seed, not its reviser), n is one, and the §0 boundary stands.

**What is owed:** the keep/modify/drop question on the reviser is now an operator milestone
decision with real evidence: on its first within-family measurement the stage demonstrated
no sentence-axis win the battery would accept, at 109% of writer cost, with its two audit
defects (gloss introduction, flat subordination) on record. A second listing family
(~$9/arm) would double the pattern or break it, if the operator wants n=2 before deciding.
